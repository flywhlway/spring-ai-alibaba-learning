server:
  port: 18001

spring:
  application:
    name: quickstart-demo
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-plus
          temperature: 0.7

logging:
  level:
    com.alibaba.cloud.ai: INFO
