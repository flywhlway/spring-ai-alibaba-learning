server:
  port: 18017

spring:
  application:
    name: redis-memory-demo
  data:
    redis:
      host: 127.0.0.1
      port: 6379
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-plus
