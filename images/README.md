# images —— 文档静态资源

存放教程与 Demo 的运行截图、导出图片。目录约定：

```
images/
├── tutorial/<章号>/        # 教程章节截图
├── examples/<demo-name>/   # Demo 运行结果截图
└── projects/<project>/     # 企业项目界面/看板截图
```

命名：`<序号>-<语义>.png`（如 `01-chat-response.png`）。架构/流程图一律使用文档内 Mermaid，不以图片形式提交（保证可维护与可 diff）。
