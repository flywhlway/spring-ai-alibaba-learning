---
phase: 06-smart-cs-platform
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/SemanticCacheService.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/FaqAnswerService.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/prompt/PromptTemplateProvider.java
autonomous: true
requirements:
  - REQ-phase-6-smart-cs
user_setup:
  - service: dashscope
    why: FAQ ETL Embedding 与 RAG 生成
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
  - service: docker
    why: Milvus + ES + Redis Stack 联调
    env_vars: []
must_haves:
  truths:
    - FAQ 语义缓存命中时秒回答案且 cache_hit=true
    - 缓存未命中时 Milvus+ES RRF 混合检索后 RAG 生成答案并回写缓存
    - FAQ 种子 ETL 双写 Milvus scs_faq 与 ES scs-faq，faq_chunk 记录溯源 ID
  artifacts:
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/HybridSearchService.java
      provides: RRF 混合检索
      contains: "RRF_K"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/SemanticCacheService.java
      provides: Redis Stack 语义缓存
      contains: "similarityThreshold"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/FaqAnswerService.java
      provides: 缓存→混合→RAG 链路
      contains: "lookup"
  key_links:
    - from: FaqAnswerService
      to: SemanticCacheService
      via: lookup 优先
      pattern: "semanticCacheService\\.lookup"
    - from: HybridSearchService
      to: milvusVectorStore + elasticsearchVectorStore
      via: 双路检索 RRF 融合
      pattern: "accumulateRrf"
---

<objective>
Wave 2 FAQ/RAG：FaqEtlPipeline 种子入库、HybridSearchService RRF、SemanticCacheService、RagPipelineFactory、FaqAnswerService 全链路。

Purpose: 满足 REQ-phase-6-smart-cs「FAQ 秒答 + Milvus + Redis 语义缓存 + ES 全文混合检索」核心路径。
Output: FAQ 问答 REST 或 Service 层可 curl 验证命中/未命中（Wave 4 接 SSE 前可先 POST /api/faq/ask 若本波添加，或 FaqAnswerService 单测验证）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smart-cs-platform/06-RESEARCH.md
@.planning/phases/06-smart-cs-platform/06-PATTERNS.md
@projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/RagPipelineFactory.java
@projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/DocumentEtlPipeline.java
@examples/26-es-hybrid-demo/src/main/java/com/flywhl/saa/eshybrid/service/HybridSearchService.java
@examples/25-redis-vector-demo/src/main/java/com/flywhl/saa/redisvector/controller/RedisVectorController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: PromptTemplateProvider + RagPipelineFactory + FaqEtlPipeline</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/prompt/PromptTemplateProvider.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/RagPipelineFactory.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/FaqEtlPipeline.java</files>
  <read_first>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/prompt/PromptTemplateProvider.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/RagPipelineFactory.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/DocumentEtlPipeline.java</read_first>
  <acceptance_criteria>
    - PromptTemplateProvider 提供 FAQ query-rewrite 与 RAG system 模板（可读 classpath 或 DB prompt_template）
    - RagPipelineFactory 装配 RetrievalAugmentationAdvisor：RewriteQueryTransformer + VectorStoreDocumentRetriever(milvusVectorStore) + score rerank + ContextualQueryAugmenter allowEmptyContext=false
    - FaqEtlPipeline：读取 db/data.sql 对应 FAQ 文本或 classpath faq-seed/*.md → TokenTextSplitter(chunkSize/chunkOverlap 来自 ScsProperties.rag) → Embedding → 双写 milvusVectorStore + elasticsearchVectorStore → 更新 faq_article.status=INDEXED、faq_chunk.milvus_pk/es_doc_id
    - 提供 @Component 方法 indexAllSeedFaqs() 供启动 CommandLineRunner 或 admin 触发（默认启动索引演示 FAQ）
  </acceptance_criteria>
  <action>
    复制 kqa RagPipelineFactory，documentRetriever 限定 milvusVectorStore @Qualifier。FaqEtlPipeline 无 MinIO，直接从 FAQ 实体 content 字段或种子文件读取。失败时 faq_article.status=FAILED 并记日志。index 元数据含 faqArticleId、chunkIndex 供 HybridSearch 回溯。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'RetrievalAugmentationAdvisor' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/RagPipelineFactory.java</automated>
  </verify>
  <done>RAG 管线与 ETL 编译绿</done>
</task>

<task type="auto">
  <name>Task 2: HybridSearchService</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/HybridSearchService.java</files>
  <read_first>examples/26-es-hybrid-demo/src/main/java/com/flywhl/saa/eshybrid/service/HybridSearchService.java</read_first>
  <acceptance_criteria>
    - 直接移植 26-demo RRF 核心：RRF_K=60，vectorSearch(milvusVectorStore) + fullTextSearch(elasticsearchVectorStore RestClient match)
    - hybridSearch(query, topK) 返回 List 含 documentId、score、text、metadata
    - candidateK = max(topK*2, topK)；无 Spring AI 内置 Hybrid API 依赖
  </acceptance_criteria>
  <action>
    复制 HybridSearchService 类结构，注入 @Qualifier milvusVectorStore 与 elasticsearchVectorStore。fullTextSearch 使用 Elasticsearch Java API Client 或 Spring AI ES store 底层 RestClient（与 26-demo 一致）。包名改为 com.flywhl.saa.smartcs.rag。处理 IOException 向上抛或包装 BizException。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'RRF_K' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/HybridSearchService.java</automated>
  </verify>
  <done>HybridSearchService 编译绿且含 RRF_K</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SemanticCacheService + FaqAnswerService</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/SemanticCacheService.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/FaqAnswerService.java, projects/smart-cs-platform/src/test/java/com/flywhl/saa/smartcs/service/SemanticCacheServiceTest.java</files>
  <read_first>examples/25-redis-vector-demo/src/main/java/com/flywhl/saa/redisvector/controller/RedisVectorController.java, 06-PATTERNS.md SemanticCacheService 节, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/QaService.java</read_first>
  <behavior>
    - SemanticCacheServiceTest: mock redisStackVectorStore，lookup 命中未过期 metadata 返回答案；过期条目跳过
    - FaqAnswerService：lookup 命中 → 直接返回答案 cacheHit=true；未命中 → hybridSearch + ChatClient+RAG advisor 生成 → put 缓存 cacheHit=false
  </behavior>
  <acceptance_criteria>
    - SemanticCacheService.lookup：similarityThreshold 默认 0.95（ScsProperties.cache），filter type=semantic-cache，校验 expiresAt ISO-8601
    - SemanticCacheService.put：metadata answer、expiresAt=now+ttlSeconds
    - FaqAnswerService.answer(query) 返回 ChatAnswerVO(answer, cacheHit, citations 可选)
    - 链路顺序严格：缓存 → hybrid → RAG → 回写缓存
  </acceptance_criteria>
  <action>
    从 25-demo 抽取 lookup/put 为 SemanticCacheService，注入 redisStackVectorStore 与 ScsProperties。FaqAnswerService 组合 SemanticCacheService、HybridSearchService、ChatClient（带 RetrievalAugmentationAdvisor）、ScsProperties。先写 SemanticCacheServiceTest（RED→GREEN）。可选添加 FaqController POST /api/faq/ask 便于本波 curl 验收（@PreAuthorize CUSTOMER+）。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q test -Dtest=SemanticCacheServiceTest &amp;&amp; mvn -f projects/smart-cs-platform/pom.xml -q compile</automated>
  </verify>
  <done>语义缓存单测绿；FaqAnswerService 链路实现完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query→RAG | 未过滤用户输入进入检索与 LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | FaqAnswerService | mitigate | allowEmptyContext=false；系统 Prompt 边界 |
| T-06-05 | Information Disclosure | 语义缓存 | accept | 仅缓存 FAQ 答案，TTL 默认 300s |
| T-06-SC | Tampering | 向量索引 | mitigate | ETL 元数据 type 过滤 |
</threat_model>

<verification>
- [ ] compile + SemanticCacheServiceTest 绿
- [ ] HybridSearchService 含 RRF
- [ ] FaqAnswerService 调用顺序 cache→hybrid→RAG
</verification>

<success_criteria>
FAQ 三件套（语义缓存 + 混合检索 + RAG）Service 层就绪，可供 Agent faq-agent 与 Wave 4 会话调用
</success_criteria>

<output>
Create `.planning/phases/06-smart-cs-platform/06-03-SUMMARY.md` when done
</output>
