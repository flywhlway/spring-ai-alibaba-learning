---
phase: 06-smart-cs-platform
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/CsOrchestratorService.java
autonomous: true
requirements:
  - REQ-phase-6-smart-cs
user_setup:
  - service: dashscope
    why: LlmRoutingAgent 与子 Agent 真机调用
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
must_haves:
  truths:
    - LlmRoutingAgent 路由 FAQ / business-supervisor / ticket-agent / human-escalation 四分支
    - business-supervisor 使用 ReactAgent + AgentTool.create 调度订单/售后/技术子 Agent
    - HumanInTheLoopHook.approvalOn 挂载人工升级 Agent（禁止 interruptBefore）
    - CsOrchestratorService 以 conversationId 作 threadId 调用路由 Agent
  artifacts:
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/CsAgentConfig.java
      provides: 全 Agent Bean 图
      contains: "LlmRoutingAgent"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/FlowStateExtractor.java
      provides: 提取 AssistantMessage 文本
      contains: "extractText"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/HandoffTools.java
      provides: requestHumanHandoff @Tool
      contains: "requestHumanHandoff"
  key_links:
    - from: CsOrchestratorService
      to: csIntentRouter
      via: RunnableConfig.threadId(conversationId)
      pattern: "threadId"
    - from: businessSupervisor
      to: orderAgent/afterSalesAgent/techSupportAgent
      via: AgentTool.create
      pattern: "AgentTool\\.create"
---

<objective>
Wave 3 Agent 编排：CsAgentConfig（LlmRoutingAgent + Supervisor + 子 Agent + Parallel FAQ 上下文 + HITL）、Tool 族、FlowStateExtractor、CsOrchestratorService。

Purpose: 满足 REQ-phase-6-smart-cs「RoutingAgent + Supervisor + Handoffs」——使用 JAR 真 API（LlmRoutingAgent、ReactAgent、AgentTool.create、HumanInTheLoopHook.approvalOn）。
Output: CsOrchestratorService.invoke(conversationId, question) 可返回路由后文本；grep 无废弃 Agent API。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smart-cs-platform/06-RESEARCH.md
@.planning/phases/06-smart-cs-platform/06-PATTERNS.md
@examples/41-multi-agent-demo/src/main/java/com/flywhl/saa/multiagent/config/MultiAgentConfig.java
@examples/42-supervisor-demo/src/main/java/com/flywhl/saa/supervisor/config/OfficeSupervisorConfig.java
@examples/37-agent-hitl-demo/src/main/java/com/flywhl/saa/agenthitl/config/HitlAgentConfig.java
@projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/agent/ApprovalAgentConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tool 族 + ToolSecuritySupport</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/ToolSecuritySupport.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/OrderTool.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/TicketTools.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/HandoffTools.java</files>
  <read_first>projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/tool/ToolSecuritySupport.java, projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/tool/SqlQueryTool.java, examples/37-agent-hitl-demo/src/main/java/com/flywhl/saa/agenthitl/tool/HighRiskTools.java</read_first>
  <acceptance_criteria>
    - ToolSecuritySupport：ToolContext 注入 userId/role，requireRole 校验 CUSTOMER/AGENT/ADMIN
    - OrderTool：演示查询订单状态（可读 cs_ticket 或内存演示表），SQL 白名单或固定演示数据
    - TicketTools：createTicket、queryTicketByNo、urgeTicket @Tool，委托 TicketService（Wave 4 完整状态机前可先实现 create 草稿）
    - HandoffTools.requestHumanHandoff(reason, ToolContext)：触发工单 PENDING_HUMAN 或返回 escalation 令牌供 HITL
    - 禁止 FunctionCallback
  </acceptance_criteria>
  <action>
    复制 office ToolSecuritySupport。OrderTool 演示级：根据 orderNo 返回物流状态 JSON 字符串。TicketTools 注入 TicketService（本任务可实现 TicketService 最小 create/find 供 Tool 使用，完整状态机在 06-05）。HandoffTools 调用 ticketService.createOrEscalate。所有 @Tool 方法校验 role。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -c '@Tool' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/tool/ | awk -F: '{s+=$2} END {exit (s>=4)?0:1}'</automated>
  </verify>
  <done>≥4 个 @Tool 方法编译通过</done>
</task>

<task type="auto">
  <name>Task 2: CsAgentConfig — 子 Agent + Routing + Supervisor + Parallel</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/CsAgentConfig.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/FlowStateExtractor.java</files>
  <read_first>examples/41-multi-agent-demo/src/main/java/com/flywhl/saa/multiagent/config/MultiAgentConfig.java, examples/42-supervisor-demo/src/main/java/com/flywhl/saa/supervisor/config/OfficeSupervisorConfig.java, examples/41-multi-agent-demo/src/main/java/com/flywhl/saa/multiagent/util/FlowStateExtractor.java</read_first>
  <acceptance_criteria>
    - faqAgent：ReactAgent，tools 可调 FaqAnswerService 或内嵌 FAQ 指令；可选挂载 ParallelAgent（knowledgeBaseAgent + ticketHistoryAgent）mergeOutputKey mergedContext
    - businessSupervisor：ReactAgent + AgentTool.create(orderAgent, afterSalesAgent, techSupportAgent)
    - ticketAgent：ReactAgent + TicketTools
    - humanEscalationAgent：ReactAgent + HandoffTools + HumanInTheLoopHook.approvalOn("requestHumanHandoff", ...)
    - csIntentRouter：LlmRoutingAgent.builder().subAgents(List.of(faqAgent, businessSupervisor, ticketAgent, humanEscalationAgent)) + ModelCallLimitHook runLimit 6
    - FlowStateExtractor.extractText(Optional&lt;OverAllState&gt;) 复制 41-demo
    - 禁止 SupervisorAgent、AgentTool.from、LlmRoutingAgent.routes(Map)
  </acceptance_criteria>
  <action>
    按 06-PATTERNS CsAgentConfig 节逐 Bean 实现。子 Agent systemPrompt 中文描述职责。humanEscalationAgent 使用 MemorySaver + HITL hook（37-demo 模式）。order/afterSales/techSupport 为独立 ReactAgent Bean，各挂领域 @Tool。faqAgent 可注入 FaqAnswerService 作为 @Tool 封装 answer 方法。编译后 rg 废弃 API 为零。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'LlmRoutingAgent' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/agent/CsAgentConfig.java &amp;&amp; rg -v '^#' projects/smart-cs-platform/src/main/java | rg -c 'SupervisorAgent|AgentTool\\.from|interruptBefore' | grep -q '^0$'</automated>
  </verify>
  <done>LlmRoutingAgent + Supervisor + 四子 Agent 编译绿；废弃 API 零命中</done>
</task>

<task type="auto">
  <name>Task 3: CsOrchestratorService</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/CsOrchestratorService.java</files>
  <read_first>projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/service/ApprovalService.java, examples/41-multi-agent-demo/src/main/java/com/flywhl/saa/multiagent/controller/MultiAgentController.java</read_first>
  <acceptance_criteria>
    - invoke(conversationId, question)：RunnableConfig.threadId=conversationId（UUID 字符串，禁止自增）
    - 调用 csIntentRouter.invoke(query, config) → FlowStateExtractor.extractText
    - 检测 InterruptionMetadata：返回结构化结果含 interrupted=true、pendingTools，供 Wave 4 HITL Controller 使用
    - routeAgent 名称写入 cs_message 元数据（常量枚举 FAQ/BUSINESS/TICKET/HUMAN）
  </acceptance_criteria>
  <action>
    CsOrchestratorService 注入 LlmRoutingAgent csIntentRouter、FaqAnswerService（FAQ 直答备用路径可选）。invoke 返回 OrchestratorResult(text, routeAgent, interrupted, interruptionMetadata)。不实现 SSE（属 Wave 4）。处理 GraphRunnerException 转 BizException。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'FlowStateExtractor' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/CsOrchestratorService.java</automated>
  </verify>
  <done>CsOrchestratorService 编译绿，可提取路由文本与 HITL 中断态</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM→Tool | 模型触发 @Tool 越权 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Elevation | TicketTools/HandoffTools | mitigate | ToolSecuritySupport.requireRole |
| T-06-07 | Tampering | Agent 编排 | mitigate | ModelCallLimitHook；禁止废弃 API |
| T-06-SC | Tampering | agent-framework | mitigate | BOM 锁定 1.1.2.2 |
</threat_model>

<verification>
- [ ] LlmRoutingAgent + AgentTool.create 存在
- [ ] HumanInTheLoopHook.approvalOn 存在
- [ ] 废弃 Agent API grep 为零
</verification>

<success_criteria>
多智能体编排层就绪，Wave 4 可接 SSE 会话与 HITL Controller
</success_criteria>

<output>
Create `.planning/phases/06-smart-cs-platform/06-04-SUMMARY.md` when done
</output>
