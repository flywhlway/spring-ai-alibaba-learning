---
phase: 06-smart-cs-platform
plan: 09
type: execute
wave: 2
depends_on:
  - 06-08
files_modified:
  - projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st
  - projects/smart-cs-platform/db/data.sql
  - projects/smart-cs-platform/src/test/resources/db/data.sql
  - projects/smart-cs-platform/README.md
autonomous: true
gap_closure: true
requirements:
  - REQ-phase-6-smart-cs
user_setup:
  - service: docker
    why: Docker 可用时 G-06-09-health 为硬门禁（force-recreate init + spring-boot:run + curl health）；不可用则 residual
    env_vars: []
  - service: dashscope
    why: 全链路 uat-smart-cs.sh 可选；health 本身不依赖模型 Key
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
must_haves:
  truths:
    - "classpath prompts/query-rewrite.st 同时含 {target} 与 {query}"
    - "db/data.sql 与 test 副本种子同时含 {target} 与 {query}，且对已有库有幂等 UPDATE"
    - "RewriteQueryTransformer 构造不再因缺 target 抛 IllegalArgumentException（Bean 可创建）"
    - "Docker 可用时 actuator/health 为 UP；不可用时 SUMMARY 显式 residual risk（G-06-09-health）"
  artifacts:
    - path: projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st
      provides: 合规查询改写模板
      contains: "{target}"
    - path: projects/smart-cs-platform/db/data.sql
      provides: query-rewrite INSERT + 幂等 UPDATE
      contains: "{target}"
    - path: projects/smart-cs-platform/src/test/resources/db/data.sql
      provides: IT 种子与主库一致
      contains: "{target}"
  key_links:
    - from: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/RagPipelineFactory.java
      to: PromptTemplateProvider.getQueryRewriteTemplate
      via: RewriteQueryTransformer.builder().promptTemplate(...)
      pattern: "getQueryRewriteTemplate"
    - from: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/prompt/PromptTemplateProvider.java
      to: prompt_template 表 / classpath query-rewrite.st
      via: get("query-rewrite") DB PUBLISHED 优先
      pattern: "query-rewrite"
---

<objective>
关闭 UAT Gap：query-rewrite Prompt 缺少 Spring AI 1.1.2 `RewriteQueryTransformer` 强制占位符 `{target}`，导致 `retrievalAugmentationAdvisor` Bean 创建失败。

Purpose: 冷启动 blocker #2——挂载修好后仍会因 PromptAssert 起不来；DB PUBLISHED 优先于 classpath，必须双改 + 已有库幂等修复。
Output: 合规 `.st` + `db/data.sql`（含幂等 UPDATE）+ test 副本；README 说明已有库如何重跑 init；不改 RagPipelineFactory 注入方式（模板合规即可）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smart-cs-platform/06-HUMAN-UAT.md
@.planning/phases/06-smart-cs-platform/06-VALIDATION.md
@.planning/phases/06-smart-cs-platform/06-PATTERNS.md
@.planning/debug/query-rewrite-target.md
@projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st
@projects/smart-cs-platform/db/data.sql
@projects/smart-cs-platform/src/test/resources/db/data.sql
@projects/knowledge-qa-platform/db/data.sql
@projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/rag/RagPipelineFactory.java
@projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/prompt/PromptTemplateProvider.java
@.claude/skills/saa-conventions/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补齐 classpath 与种子库 query-rewrite 的 {target}+{query}</name>
  <files>projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st, projects/smart-cs-platform/db/data.sql, projects/smart-cs-platform/src/test/resources/db/data.sql</files>
  <read_first>projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st, projects/smart-cs-platform/db/data.sql, projects/smart-cs-platform/src/test/resources/db/data.sql, projects/knowledge-qa-platform/db/data.sql, .planning/debug/query-rewrite-target.md, .planning/phases/06-smart-cs-platform/06-PATTERNS.md</read_first>
  <action>
    Spring AI 1.1.2 `RewriteQueryTransformer` 构造时 `PromptAssert.templateHasRequiredPlaceholders` 强制模板同时含 `{target}` 与 `{query}`（见 debug/query-rewrite-target.md）。当前三处仅有 `{query}`。

    PATTERNS analog（06-PATTERNS.md）：`db/data.sql` → `projects/knowledge-qa-platform/db/data.sql`（exact）；`prompt/PromptTemplateProvider` / `rag/RagPipelineFactory` → kqa 同名类。种子风格对齐 kqa：`prompt_template` INSERT + `ON CONFLICT (template_key, version) DO NOTHING`；本 gap 在其后追加幂等 UPDATE（kqa 无此 UPDATE，smartcs 因 Spring AI PromptAssert 必加）。RagPipelineFactory 仍按 PATTERNS 注入 `promptTemplateProvider.getQueryRewriteTemplate()`，只改模板正文不改 Java。

    统一中文模板语义（对齐默认模板「rewrite for querying a {target}」），三处内容必须一致，例如：

    将用户问题改写为适合查询 {target} 的独立检索语句，保留关键实体与限定词，输出改写后的查询本身，不要解释。

    原问题：
    {query}

    修改文件：
    1. `src/main/resources/prompts/query-rewrite.st` — 全文替换为上述模板（保留换行，勿只改 classpath 而漏 DB）。
    2. `db/data.sql` — 更新 `query-rewrite` 的 INSERT content 为同一模板；因 `ON CONFLICT (template_key, version) DO NOTHING` 不会刷新已有行，在 INSERT 块之后追加幂等 UPDATE：
       UPDATE prompt_template SET content = (与 INSERT 相同的模板正文) WHERE template_key = 'query-rewrite' AND content NOT LIKE '%{target}%';
       （无 updated_at 列，勿编造列名。）
    3. `src/test/resources/db/data.sql` — INSERT content 与主 `db/data.sql` 同步；同样追加相同幂等 UPDATE，保证 IT 基座与生产种子一致。

    不要修改 `RagPipelineFactory.java`（仍注入自定义 promptTemplate；模板合规即可通过 PromptAssert）。
    不要修改 HITL / Chat handoff（06-REVIEW Critical / D-14 另案）。
    版本锁定不变：Boot 3.5.16 / SAA 1.1.2.2 / Spring AI 1.1.2。
    验收登记见 06-VALIDATION.md 门禁 G-06-09-ph / G-06-09-upd。
  </action>
  <verify>
    <automated>for f in projects/smart-cs-platform/src/main/resources/prompts/query-rewrite.st projects/smart-cs-platform/db/data.sql projects/smart-cs-platform/src/test/resources/db/data.sql; do grep -q '{target}' "$f" && grep -q '{query}' "$f" || exit 1; done; grep -q "content NOT LIKE '%{target}%'" projects/smart-cs-platform/db/data.sql && grep -q "content NOT LIKE '%{target}%'" projects/smart-cs-platform/src/test/resources/db/data.sql</automated>
  </verify>
  <done>三处模板均含 {target}+{query}；主/test data.sql 均有针对缺 target 的幂等 UPDATE</done>
</task>

<task type="auto">
  <name>Task 2: README 已有库修复说明 + 冷启动门禁</name>
  <files>projects/smart-cs-platform/README.md</files>
  <read_first>projects/smart-cs-platform/README.md</read_first>
  <action>
    在 README「4.2 启动中间件」附近增加简短排障（勿写长文；风格对齐 PATTERNS analog `projects/knowledge-qa-platform/README.md` 中间件启动段）：
    1. volume 已改为相对 `docker/` 的 `../projects/smart-cs-platform/db`；若曾用错误挂载起过 init，需 `--force-recreate scs-db-init`。
    2. 已有 `scs_platform` 且 query-rewrite 缺 `{target}`：重跑 init（会执行 data.sql 幂等 UPDATE），或 `DROP DATABASE scs_platform` 后重建；仅改 classpath `.st` 不够（DB PUBLISHED 优先）。
    3. 确认健康：`curl http://localhost:19300/actuator/health` 期望 UP。

    **冷启动验收（硬/残差二选一，见 06-VALIDATION.md G-06-09-health）：**
    - Docker + 依赖中间件可用时为硬门禁：按 06-VALIDATION Runtime Health Gate 执行 force-recreate scs-db-init → `mvn -f projects/smart-cs-platform/pom.xml spring-boot:run` → `curl -sf http://localhost:19300/actuator/health` 含 UP，且日志无 `placeholders must be present in the prompt template: target`。失败则本 plan 未完成。
    - Docker / 中间件不可用时：必须跑通 `mvn -f projects/smart-cs-platform/pom.xml -DskipTests compile -q`，并在 SUMMARY 明确写出 residual risk：`G-06-09-health=residual — 未验证 Bean 冷启动；需后续有 Docker 会话或 uat-smart-cs.sh 补跑`。不得把「仅 compile 绿」写成冷启动已闭环。
    全链路可选：`bash projects/smart-cs-platform/scripts/uat-smart-cs.sh`（需 Key）；不要求修 HITL approve 404。
  </action>
  <verify>
    <automated>grep -q '{target}' projects/smart-cs-platform/README.md && grep -q 'force-recreate\|scs-db-init' projects/smart-cs-platform/README.md && mvn -f projects/smart-cs-platform/pom.xml -DskipTests compile -q</automated>
    <human-check>若 docker info 成功：按 06-VALIDATION Runtime Health Gate 执行 force-recreate + spring-boot:run + curl health UP（硬门禁）。若 Docker 不可用：SUMMARY 必须含字面量 G-06-09-health=residual。</human-check>
  </verify>
  <done>README 含已有库/{target}/重跑 init 说明；compile 绿；Docker 可用时 health UP（硬），否则 SUMMARY 含 G-06-09-health=residual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB seed→RewriteQueryTransformer | PUBLISHED prompt 内容进入模型改写管线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Tampering | prompt_template seed | mitigate | 仅更新演示种子文案；无用户输入拼接；保持占位符字面量 {target}/{query} |
| T-06-09-02 | Elevation of Privilege | README DROP DATABASE 提示 | accept | 本地演示排障说明；非生产自动化 |
| T-06-SC | Tampering | 无新包安装 | accept | 仅改 .st/.sql/README，无依赖变更 |
</threat_model>

<verification>
- 对齐 06-VALIDATION.md：G-06-09-ph / G-06-09-upd / G-06-09-compile
- G-06-09-health：Docker 可用时硬门禁（Runtime Health Gate）；不可用时 SUMMARY 写 `G-06-09-health=residual`
</verification>

<success_criteria>
Gap truth 可达：query-rewrite 满足 RewriteQueryTransformer 必填占位符；与 06-08 合用后，在 Docker 可用时应用冷启动 health UP；无 Docker 时 grep+compile 绿且 residual 显式记录。
</success_criteria>

<output>
Create `.planning/phases/06-smart-cs-platform/06-09-SUMMARY.md` when done
</output>
