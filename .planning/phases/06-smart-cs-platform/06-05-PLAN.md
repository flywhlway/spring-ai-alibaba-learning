---
phase: 06-smart-cs-platform
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/ChatController.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/ChatService.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/TicketService.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/TicketController.java
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/HumanHandoffController.java
autonomous: true
requirements:
  - REQ-phase-6-smart-cs
user_setup:
  - service: dashscope
    why: SSE 会话真机验收
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
must_haves:
  truths:
    - 客户 GET /api/chat/stream 获得 SSE 流式回复并持久化 cs_message
    - 工单状态机仅允许合法转移，非法转移返回 400
    - 坐席 POST /api/handoff/approve 可恢复 HITL 会话且工单进入 HUMAN_HANDLING
    - conversationId 全链路 UUID，与 RunnableConfig.threadId 一致
  artifacts:
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/ChatController.java
      provides: SSE 网关
      contains: "TEXT_EVENT_STREAM"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/TicketService.java
      provides: 工单状态机
      contains: "ALLOWED_TRANSITIONS"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/HumanHandoffController.java
      provides: HITL start/approve
      contains: "addHumanFeedback"
  key_links:
    - from: ChatService
      to: CsOrchestratorService
      via: stream/invoke 委托路由
      pattern: "orchestratorService"
    - from: HumanHandoffController
      to: humanEscalationAgent
      via: invokeAndGetOutput + resume
      pattern: "resume\\(\\)"
---

<objective>
Wave 4 会话/工单：ChatController SSE、ChatService、TicketService 完整状态机、TicketController、HumanHandoffController 人工接管 API。

Purpose: 闭环 REQ-phase-6-smart-cs「工单流转 + Graph interrupt（HITL）+ 人工接管状态机」与用户可见会话网关。
Output: curl 可验证 chat/stream、工单创建/转移、handoff start/approve。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smart-cs-platform/06-RESEARCH.md
@.planning/phases/06-smart-cs-platform/06-PATTERNS.md
@projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/QaController.java
@projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/QaService.java
@examples/37-agent-hitl-demo/src/main/java/com/flywhl/saa/agenthitl/controller/HitlController.java
@projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/service/ApprovalService.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TicketService 状态机 + TicketController</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/TicketService.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/TicketController.java, projects/smart-cs-platform/src/test/java/com/flywhl/saa/smartcs/service/TicketServiceTest.java</files>
  <read_first>06-PATTERNS.md TicketService 节, projects/office-agent-assistant/src/main/java/com/flywhl/saa/officeagent/controller/ApprovalController.java</read_first>
  <behavior>
    - TicketServiceTest: OPEN→AI_PROCESSING 合法；AI_PROCESSING→PENDING_HUMAN 合法；OPEN→CLOSED 非法抛 BizException
    - createOrEscalate：无工单则创建 OPEN→PENDING_HUMAN；写 cs_ticket_event
    - transition(ticketId, to, actor, reason) 校验 ALLOWED_TRANSITIONS
  </behavior>
  <acceptance_criteria>
    - ALLOWED_TRANSITIONS：OPEN→AI_PROCESSING；AI_PROCESSING→RESOLVED|PENDING_HUMAN；PENDING_HUMAN→HUMAN_HANDLING|AI_PROCESSING；HUMAN_HANDLING→RESOLVED；RESOLVED→CLOSED
    - ticket_no 生成规则 SCS-yyyyMMdd-序号
    - TicketController：POST /api/tickets、GET /api/tickets/{no}、PATCH /api/tickets/{id}/transition（AGENT/ADMIN）
    - 禁止客户端直写 status 字段绕过状态机
  </acceptance_criteria>
  <action>
    实现 TicketService 完整状态机与 ticket_event 审计。TicketTools 改为调用此 Service。TicketController 使用 Result&lt;TicketVO&gt; 包装。先写 TicketServiceTest RED→GREEN。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q test -Dtest=TicketServiceTest</automated>
  </verify>
  <done>TicketServiceTest 绿；Ticket REST 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: ChatController + ChatService SSE</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/ChatController.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/service/ChatService.java</files>
  <read_first>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/QaController.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/QaService.java</read_first>
  <acceptance_criteria>
    - GET /api/chat/stream?conversationId=&amp;question= 返回 Flux&lt;ServerSentEvent&lt;String&gt;&gt;
    - POST /api/chat/ask 同步问答（可选，Result&lt;ChatAnswerVO&gt;）
    - ChatService：ensureConversation（cs_conversation）、saveUserMessage/saveAssistantMessage（含 route_agent、cache_hit、token 列）
    - 流式路径：将 CsOrchestratorService 结果分块 SSE（event: message/done/meta）；中断时 event: interrupt 含 threadId
    - @PreAuthorize hasAnyRole('CUSTOMER','AGENT','ADMIN')
  </acceptance_criteria>
  <action>
    复制 kqa QaService SSE 骨架，路径改为 /api/chat。conversationId 由客户端传入 UUID 或服务端首次生成返回。委托 CsOrchestratorService.invoke；非流式 Faq 路径可直调 FaqAnswerService。使用 MessageChatMemoryAdvisor 时 conversationId 参数一致。meta 事件含 routeAgent、cacheHit。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'TEXT_EVENT_STREAM' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/ChatController.java</automated>
  </verify>
  <done>Chat SSE 端点编译绿</done>
</task>

<task type="auto">
  <name>Task 3: HumanHandoffController</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/HumanHandoffController.java</files>
  <read_first>examples/37-agent-hitl-demo/src/main/java/com/flywhl/saa/agenthitl/controller/HitlController.java, 06-PATTERNS.md HumanHandoffController 节</read_first>
  <acceptance_criteria>
    - POST /api/handoff/start：生成 UUID threadId（若未绑定 conversationId 则新建），humanEscalationAgent.invokeAndGetOutput，遇 InterruptionMetadata 缓存 pendingByThread，ticketService.transitionToPendingHuman
    - POST /api/handoff/approve?threadId=：坐席 JWT（AGENT/ADMIN），addHumanFeedback + resume()，ticketService.transitionToHumanHandling
    - threadId 与 cs_conversation.conversation_id 绑定；无 pending 返回 NOT_FOUND
    - 使用 ConcurrentHashMap 或 DB 持久化 pending（演示可用内存 Map，文档注明生产应持久化）
  </acceptance_criteria>
  <action>
    复制 37-demo HitlController start/approve 流程，注入 ReactAgent humanEscalationAgent Bean、TicketService。buildApprovedFeedback 从 InterruptionMetadata 构造 HumanFeedback。返回 HitlSessionResponse VO。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -q 'addHumanFeedback' projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/controller/HumanHandoffController.java</automated>
  </verify>
  <done>HumanHandoffController 编译绿，含 resume 路径</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ticket | 工单状态篡改 |
| client→handoff | HITL 会话劫持 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | TicketService | mitigate | 服务端 ALLOWED_TRANSITIONS 枚举校验 |
| T-06-09 | Spoofing | HumanHandoffController | mitigate | AGENT/ADMIN @PreAuthorize + UUID threadId |
| T-06-SC | Tampering | SSE 输入 | mitigate | Jakarta Validation question 长度限制 |
</threat_model>

<verification>
- [ ] TicketServiceTest 绿
- [ ] /api/chat/stream 存在
- [ ] handoff approve 含 resume()
</verification>

<success_criteria>
会话 SSE + 工单状态机 + HITL API 可用，可供 admin/ops 与 UAT 波次验收
</success_criteria>

<output>
Create `.planning/phases/06-smart-cs-platform/06-05-SUMMARY.md` when done
</output>
