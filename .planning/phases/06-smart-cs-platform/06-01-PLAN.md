---
phase: 06-smart-cs-platform
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - projects/smart-cs-platform/pom.xml
  - projects/smart-cs-platform/README.md
  - projects/smart-cs-platform/docker-compose.override.yml
  - projects/smart-cs-platform/db/schema.sql
  - projects/smart-cs-platform/db/data.sql
  - projects/smart-cs-platform/http/api.http
  - projects/smart-cs-platform/src/main/resources/application.yml
  - projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/
autonomous: true
requirements:
  - REQ-phase-6-smart-cs
user_setup: []
must_haves:
  truths:
    - smart-cs-platform 工程可独立 mvn compile，端口 19300，包根 com.flywhl.saa.smartcs
    - db/schema.sql 含工单/FAQ/model_profile 等全部业务表，与 JPA Entity 列名一致
    - docker-compose.override.yml 含 smartcs profile（scs-db-init + scs-redis-stack:6380）
    - ScsProperties 绑定 scs.rag/cache/memory/security/ticket 配置键
  artifacts:
    - path: projects/smart-cs-platform/pom.xml
      provides: 独立 Maven 工程（不挂父 POM modules）
      contains: "saa-learning-common"
    - path: projects/smart-cs-platform/db/schema.sql
      provides: PostgreSQL DDL SSOT
      contains: "scs_platform"
    - path: projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/config/ScsProperties.java
      provides: scs.* 配置绑定
      contains: "@ConfigurationProperties"
  key_links:
    - from: docker-compose.override.yml
      to: db/schema.sql
      via: scs-db-init entrypoint psql -f
      pattern: "scs-db-init"
    - from: ScsProperties
      to: application.yml
      via: "prefix = \"scs\""
      pattern: "scs\\.(rag|cache|memory)"
---

<objective>
Wave 0 地基：新建 smart-cs-platform 绿field 工程骨架——pom、README、DDL/演示数据、compose override、http 契约、ScsProperties、全量 Entity/Repository/DTO/VO/Mapper。

Purpose: 为后续 config/FAQ/Agent/会话/admin 波次提供可编译的持久化契约与中间件编排入口（REQ-phase-6-smart-cs 交付标准）。
Output: `mvn -f projects/smart-cs-platform/pom.xml compile` 通过；零 TODO/伪代码。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smart-cs-platform/06-RESEARCH.md
@.planning/phases/06-smart-cs-platform/06-PATTERNS.md
@projects/README.md
@projects/knowledge-qa-platform/pom.xml
@projects/knowledge-qa-platform/docker-compose.override.yml
@projects/knowledge-qa-platform/db/schema.sql
@CLAUDE.md
@.claude/skills/saa-conventions/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pom + Application + README + application.yml</name>
  <files>projects/smart-cs-platform/pom.xml, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/SmartCsApplication.java, projects/smart-cs-platform/README.md, projects/smart-cs-platform/src/main/resources/application.yml</files>
  <read_first>projects/knowledge-qa-platform/pom.xml, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/KnowledgeQaApplication.java, projects/knowledge-qa-platform/README.md</read_first>
  <acceptance_criteria>
    - parent 指向仓库父 POM，artifactId smart-cs-platform，**不**加入根 pom modules
    - 依赖：saa-learning-common、saa-learning-starter、spring-ai-alibaba-starter-dashscope、spring-ai-starter-model-deepseek、spring-ai-alibaba-agent-framework、spring-ai-rag、spring-ai-starter-vector-store-milvus、spring-ai-starter-vector-store-elasticsearch、spring-ai-alibaba-starter-nacos-prompt、spring-boot-starter-oauth2-resource-server、spring-boot-starter-data-jpa、spring-boot-starter-data-redis、spring-boot-starter-web、spring-boot-starter-actuator、knife4j-openapi3-jakarta-spring-boot-starter、postgresql、lombok、mapstruct；test：spring-boot-starter-test、testcontainers（junit-jupiter、postgresql、elasticsearch 可选）
    - server.port=19300；spring.jpa.hibernate.ddl-auto=none
    - 密钥占位 ${AI_DASHSCOPE_API_KEY}，禁止硬编码
  </acceptance_criteria>
  <action>
    新建绿field 工程。复制 kqa pom 父节点与 common/starter 模式，追加 Phase 6 增量依赖（agent-framework、elasticsearch vector store），子模块零版本号。SmartCsApplication 加 @SpringBootApplication、@EnableConfigurationProperties(ScsProperties.class)、@EnableJpaRepositories、@EnableTransactionManagement。README 含：业务场景、端口 19300、演示账号（customer1/agent1/admin + 密码）、infra 启动命令（core+vector+search+cloud+smartcs profiles）、mvn -f 编译运行示例。application.yml 骨架：datasource 指向 scs_platform、redis 6379、scs.cache.redis 指向 6380、milvus collection scs_faq、elasticsearch index scs-faq、embedding text-embedding-v3 dimensions=1024、saa.learning.cost-tracking 占位、management.prometheus 占位。确认父 POM modules 未新增 smart-cs-platform。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q -DskipTests compile 2>&1 | tail -1; test -f projects/smart-cs-platform/pom.xml &amp;&amp; rg -q '19300' projects/smart-cs-platform/src/main/resources/application.yml</automated>
  </verify>
  <done>pom 与主类存在；application.yml 含 19300 与 scs_platform 数据源键</done>
</task>

<task type="auto">
  <name>Task 2: DDL + 演示数据 + compose override + api.http</name>
  <files>projects/smart-cs-platform/db/schema.sql, projects/smart-cs-platform/db/data.sql, projects/smart-cs-platform/docker-compose.override.yml, projects/smart-cs-platform/http/api.http</files>
  <read_first>projects/knowledge-qa-platform/db/schema.sql, projects/knowledge-qa-platform/db/data.sql, projects/knowledge-qa-platform/docker-compose.override.yml, examples/25-redis-vector-demo/docker-compose.override.yml</read_first>
  <acceptance_criteria>
    - schema.sql 表：sys_user、faq_article、faq_chunk、cs_conversation、cs_message、cs_ticket、cs_ticket_event、prompt_template、model_profile、audit_log、cs_feedback
    - 角色枚举 CUSTOMER/AGENT/ADMIN；工单状态 OPEN/AI_PROCESSING/PENDING_HUMAN/HUMAN_HANDLING/RESOLVED/CLOSED
    - data.sql：2 客户 + 1 坐席 + 1 admin；10+ FAQ 种子；2 条历史工单
    - scs-redis-stack 映射 6380:6379；scs-db-init 创建库 scs_platform 并执行 schema/data
    - api.http 覆盖 login、chat、ticket、handoff、admin 路径占位
  </acceptance_criteria>
  <action>
    按 06-RESEARCH DB Schema 设计编写 schema.sql（文件头注释标明 SSOT 与 scs_platform）。复用 kqa prompt_template/audit_log 列结构。data.sql 插入 BCrypt 或 {noop} 演示密码（与 kqa 一致模式）。docker-compose.override.yml：profile smartcs 含 scs-db-init（pgvector/pgvector:pg16，等待 postgres healthy 后 createdb scs_platform + psql -f schema/data）与 scs-redis-stack（redis/redis-stack-server:latest，6380:6379，healthcheck redis-cli ping）。可选 monitor profile 留注释占位（Wave 5 完善）。http/api.http：变量 baseUrl=http://localhost:19300，分组 Auth/Chat/Ticket/Handoff/Admin，与 README 演示流程对齐。
  </action>
  <verify>
    <automated>test -f projects/smart-cs-platform/db/schema.sql &amp;&amp; rg -c 'CREATE TABLE' projects/smart-cs-platform/db/schema.sql | awk '{print $1}' | xargs test 11 -le; rg -q 'scs-redis-stack' projects/smart-cs-platform/docker-compose.override.yml &amp;&amp; rg -q '6380:6379' projects/smart-cs-platform/docker-compose.override.yml</automated>
  </verify>
  <done>11 张表 DDL、演示数据、compose override 与 api.http 就绪</done>
</task>

<task type="auto">
  <name>Task 3: ScsProperties + Entity + Repository + DTO/VO + Mapper</name>
  <files>projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/config/ScsProperties.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/model/entity/*.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/repository/*.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/model/dto/*.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/model/vo/*.java, projects/smart-cs-platform/src/main/java/com/flywhl/saa/smartcs/mapper/*.java</files>
  <read_first>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/KqaProperties.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/model/entity/SysUser.java, 06-PATTERNS.md ScsProperties 节</read_first>
  <acceptance_criteria>
    - ScsProperties record：Rag(topK/similarityThreshold/chunkSize/chunkOverlap)、Cache(similarityThreshold 默认 0.95/ttlSeconds/redisUri)、Memory(maxMessages/ttl)、Security(Jwt)、Ticket(escalation 相关默认值)
    - 11 Entity @Table 列名与 schema.sql 一致；TIMESTAMPTZ→OffsetDateTime；JSON 列用 @JdbcTypeCode(SqlTypes.JSON)
    - JpaRepository 派生查询覆盖 conversation_id、ticket_no、template_key、profile_key+scene 等索引列
    - DTO：LoginRequest、ChatRequest、TicketCreateRequest、HandoffApproveRequest、PromptSaveRequest、ModelProfileSaveRequest、FeedbackRequest（jakarta.validation）
    - VO：ChatAnswerVO、TicketVO、DashboardStatsVO、FaqArticleVO、ModelProfileVO、HitlSessionResponse 等
    - MapStruct @Mapper(componentModel=spring)：UserConverter、TicketConverter、FaqConverter、MessageConverter
    - 全部 @author flywhl；零 TODO
  </acceptance_criteria>
  <action>
    实现 ScsProperties 紧凑构造器默认值（cache.similarityThreshold=0.95、memory.ttl=7d，per 06-RESEARCH）。为每张表实现 Entity 与 Repository。DTO/VO 字段对齐 api.http 与 ROADMAP 成功标准。枚举 TicketStatus、UserRole、FaqArticleStatus 放 model 包。小 record 若仅单处使用可放主包（per CLAUDE.md 同包原则）。编译前勿实现 Service/Controller 业务逻辑。
  </action>
  <verify>
    <automated>mvn -f projects/smart-cs-platform/pom.xml -q compile &amp;&amp; rg -v '^#' projects/smart-cs-platform/src/main/java | rg -ci 'TODO|FIXME|骨架占位' | grep -q '^0$'</automated>
  </verify>
  <done>数据层编译绿；Entity 数量与 schema 表数一致；无 TODO</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码→Maven | 绿field 工程首次编译失败阻塞全阶段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | db/schema.sql | mitigate | JPA ddl-auto=none，DDL 为 SSOT |
| T-06-SC | Tampering | Maven 依赖 | mitigate | 父 BOM 管理，无 npm 包 |
</threat_model>

<verification>
- [ ] `mvn -f projects/smart-cs-platform/pom.xml compile` 成功
- [ ] schema.sql ≥11 CREATE TABLE
- [ ] compose override 含 scs-db-init 与 scs-redis-stack:6380
- [ ] rg 无 TODO/伪代码
</verification>

<success_criteria>
Wave 0 完成；config 波次可注入 Repository 与 ScsProperties
</success_criteria>

<output>
Create `.planning/phases/06-smart-cs-platform/06-01-SUMMARY.md` when done
</output>
