---
phase: 07-production
plan: 05
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REQ-phase-7-production
must_haves:
  truths:
    - "本机 bash scripts/quality-gate.sh 退出 0"
    - "ci.yml / model-it.yml / deploy-smoke / 生产化文档 / UAT 索引均存在且交叉一致"
    - "ROADMAP Phase 7 Success Criteria 三条可对照交付物勾选说明写入 SUMMARY"
  artifacts:
    - path: scripts/quality-gate.sh
      provides: 最终门禁入口
      contains: "quality-gate OK"
    - path: .github/workflows/ci.yml
      provides: blocking CI
      contains: "quality-gate"
    - path: docs/00-overview/05-生产化与运维.md
      provides: 生产化文档
      min_lines: 40
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/quality-gate.sh
      via: CI job
      pattern: "quality-gate\\.sh"
    - from: docs/00-overview/05-生产化与运维.md
      to: scripts/quality-gate.sh
      via: 文档命令
      pattern: "scripts/quality-gate"
---

<objective>
做 Phase 7 收口验证：本地跑通 quality-gate，核对 CI/文档/UAT 索引齐套，更新 ROADMAP/REQUIREMENTS/STATE 进度元数据（不执行业务功能开发）。

Purpose: 证明 REQ-phase-7-production 与 ROADMAP Success Criteria 可被自动化与文档路径满足。
Output: 验证通过记录、规划状态更新、07-05-SUMMARY。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-production/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@scripts/quality-gate.sh
@.github/workflows/ci.yml
@.github/workflows/model-it.yml
@scripts/deploy-smoke.sh
@docs/00-overview/05-生产化与运维.md
@docs/00-overview/06-UAT债务索引.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 本地全量门禁与产物清单核对</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
在仓库根执行并记录结果到 SUMMARY：

1. `bash scripts/quality-gate.sh`（必须 exit 0）。
2. 存在性检查：`.github/workflows/ci.yml`、`.github/workflows/model-it.yml`、`scripts/deploy-smoke.sh`、`docs/00-overview/05-生产化与运维.md`、`docs/00-overview/06-UAT债务索引.md`。
3. 静态一致性：`ci.yml` 调用 `quality-gate.sh`；`model-it.yml` 无 job-level `if: secrets.`；`05` 文档含门禁与 infra；`06` 索引含 uat 脚本；STATE 含 06-REVIEW 债务。
4. 抽样（非默认 CI 全矩阵）：`mvn -B -f examples/01-quickstart-demo/pom.xml -DskipTests compile` 与 `mvn -B -f projects/smart-cs-platform/pom.xml test`（无 Key；IT 按 Docker 条件）。失败则修至绿再继续（优先修测试/脚本，不关扫描）。
5. 更新 `.planning/ROADMAP.md` Phase 7 进度（计划勾选状态按实际 SUMMARY 存在情况）；将 REQUIREMENTS 中 `REQ-phase-7-production` 勾选仅当本验证全过；更新 STATE 当前位置与 Last activity。

禁止：声称「GitHub 上已绿」除非已 push 且检查通过——本地验证即可收口规划执行；远程 Actions 首次绿可作为人工备注。禁止执行 K8s 或 Phase 6 Critical 代码修复。
  </action>
  <verify>
    <automated>bash scripts/quality-gate.sh && test -f .github/workflows/ci.yml && test -f .github/workflows/model-it.yml && test -f scripts/deploy-smoke.sh && test -f docs/00-overview/05-生产化与运维.md && test -f docs/00-overview/06-UAT债务索引.md && ! grep -E '^\s+if:.*secrets\.' .github/workflows/model-it.yml</automated>
  </verify>
  <done>quality-gate 本地全绿；五大交付物存在；ROADMAP/REQUIREMENTS/STATE 已按验证结果更新。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工确认生产化收口可读可跑</name>
  <files>docs/00-overview/05-生产化与运维.md, docs/00-overview/06-UAT债务索引.md, .planning/STATE.md</files>
  <what-built>
    Phase 7 全套：quality-gate、GitHub Actions（ci + model-it）、deploy-smoke、生产化文档、UAT 债务索引；本地 quality-gate 已自动跑绿。
  </what-built>
  <action>
暂停等待人工确认（checkpoint）。按 how-to-verify 逐步检查文档与可选远程 CI/smoke；用户回复 approved 或返工清单后继续。不在本任务修改业务代码。
  </action>
  <how-to-verify>
    1. 打开 `docs/00-overview/05-生产化与运维.md`，确认 CI/门禁/部署/排障章节可读。
    2. 打开 `docs/00-overview/06-UAT债务索引.md`，确认 Phase 4/5/6 入口正确。
    3. （可选）若已 push 到 GitHub：在 Actions 页确认 `ci` workflow 出现；无 Key 时仍应绿。
    4. （可选）`bash scripts/deploy-smoke.sh` 对单一项目做 smoke（需 Docker；Milvus 需等待）。
    5. 确认 STATE 中 Phase 6 Critical 债务仍在、且未要求本阶段修代码。
  </how-to-verify>
  <verify>
    <automated>test -f docs/00-overview/05-生产化与运维.md && test -f docs/00-overview/06-UAT债务索引.md && grep -v '^#' .planning/STATE.md | grep -c '06-REVIEW' | grep -qE '^[1-9]'</automated>
  </verify>
  <done>用户回复 approved；或记录返工项供后续 plan 修复。</done>
  <resume-signal>Type "approved" 或列出需返工项</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地验证 → 规划状态文件 | 仅更新 .planning 元数据；不写入 secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | 提前勾选 REQ | mitigate | 仅在 quality-gate 与产物检查通过后勾选 REQUIREMENTS |
| T-07-SC | Tampering | 无新依赖 | accept | 验证计划不安装包 |
</threat_model>

<verification>
- 本计划 Task 1 automated 命令即 phase gate
- 人工 checkpoint 确认文档与可选远程 CI
</verification>

<success_criteria>
- ROADMAP Phase 7 Success Criteria：
  1. CI/CD + 部署脚本路径存在并可构建
  2. quality-gate 一键覆盖编译抽样 + version-audit + readiness 阈值
  3. §7 扫描无废弃 API / 硬编码密钥 / TODO
- curl 真机路径经 UAT 索引与既有 uat 脚本可发现（非默认 CI）
</success_criteria>

<output>
Create `.planning/phases/07-production/07-05-SUMMARY.md` when done
</output>
