---
phase: 07-production
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - scripts/deploy-smoke.sh
  - docs/00-overview/05-生产化与运维.md
  - docs/README.md
  - README.md
autonomous: true
requirements:
  - REQ-phase-7-production
must_haves:
  truths:
    - "学习者可按文档用 Compose + infra.sh profiles 拉起中间件并启动三企业项目之一"
    - "deploy-smoke 骨架支持 wait/retry 健康检查，规避 Milvus 冷启动误杀（Pitfall 6）"
    - "不引入 K8s/Helm；Dockerfile 仅可选文档说明（D-10/D-11）"
    - "生产化文档含 CI 说明、门禁命令、部署步骤、常见排障"
  artifacts:
    - path: scripts/deploy-smoke.sh
      provides: 一键起中间件后的健康 smoke 骨架
      contains: "actuator/health"
    - path: docs/00-overview/05-生产化与运维.md
      provides: Phase 7 生产化章节（CI/门禁/部署/排障）
      contains: "quality-gate"
    - path: docs/README.md
      provides: 总览索引链到 05 文档
      contains: "05-生产化与运维"
  key_links:
    - from: docs/00-overview/05-生产化与运维.md
      to: scripts/infra.sh
      via: 文档引用 profiles
      pattern: "infra\\.sh"
    - from: scripts/deploy-smoke.sh
      to: projects/*/actuator
      via: curl health
      pattern: "19100|19200|19300"
    - from: README.md
      to: docs/00-overview/05-生产化与运维.md
      via: 链接
      pattern: "05-生产化"
---

<objective>
交付 Docker Compose 类生产部署路径：`deploy-smoke.sh` 骨架 + `docs/00-overview/05-生产化与运维.md`，并挂到 docs/根 README。

Purpose: 实现 D-10/D-11/D-12；教学仓「一键可验证」而非云上 K8s。
Output: deploy-smoke 脚本、生产化运维文档、索引更新。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-production/07-CONTEXT.md
@.planning/phases/07-production/07-RESEARCH.md
@scripts/infra.sh
@projects/knowledge-qa-platform/README.md
@projects/office-agent-assistant/README.md
@projects/smart-cs-platform/README.md
@projects/README.md
@docs/README.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 deploy-smoke.sh 骨架</name>
  <files>scripts/deploy-smoke.sh</files>
  <action>
新建 `scripts/deploy-smoke.sh`（discretion skeleton，per RESEARCH Pattern 4 / D-10），`set -euo pipefail`。

行为约定：
- 参数：项目选择（`kqa|office|smartcs` 或目录名）与可选 `skip-infra`。
- 中间件：调用既有 `bash scripts/infra.sh up ...` 或文档等价的 `docker compose -f docker/docker-compose.yml -f projects/&lt;proj&gt;/docker-compose.override.yml --profile ... up -d`。复用 `infra.sh` / 各项目 README 已写明的 profile 组合，禁止新写一套 compose 编排（Don't Hand-Roll）。
- 等待：对依赖服务做 retry/sleep（Milvus 30~60s，Pitfall 6）；可用 `docker compose ps` / 循环直到 healthy 或超时非零退出。
- 应用：`mvn -B -f projects/&lt;proj&gt;/pom.xml -DskipTests package` 后提示 `java -jar` 或后台拉起；再 `curl -sf` 对应端口 `/actuator/health`（19100/19200/19300）。
- 脚本头部注释写明：需本机 Docker；真机模型调用另需 `AI_DASHSCOPE_API_KEY`；演示口令须替换（Security Domain）。

禁止：引入 Helm/K8s manifests（D-11）；本阶段强制新增三份 Dockerfile（A5：文档 + compose 足够；若文档提及镜像，仅写「可选：自行 `spring-boot:build-image` / 手写 Dockerfile」一段即可）。
  </action>
  <verify>
    <automated>bash -n scripts/deploy-smoke.sh && grep -v '^#' scripts/deploy-smoke.sh | grep -cE 'actuator/health|19100|19200|19300' | grep -qE '^[1-9]' && grep -v '^#' scripts/deploy-smoke.sh | grep -cE 'infra\.sh|docker compose' | grep -qE '^[1-9]'</automated>
  </verify>
  <done>deploy-smoke.sh 语法正确；含 compose/infra 路径、健康检查端口与 retry 语义说明或实现。</done>
</task>

<task type="auto">
  <name>Task 2: 撰写 05-生产化与运维.md 并更新索引</name>
  <files>docs/00-overview/05-生产化与运维.md, docs/README.md, README.md</files>
  <action>
新建 `docs/00-overview/05-生产化与运维.md`（per D-12），图示一律 Mermaid（saa-conventions）。至少包含：

1. **CI 说明**：指向 `.github/workflows/ci.yml` / `model-it.yml`；说明默认无 Key、optional 模型 IT 需 Secret + workflow_dispatch。
2. **质量门禁**：`bash scripts/quality-gate.sh`；对齐 HANDOFF §7 清单；说明 curl/真机 UAT 走既有 `scripts/uat-*.sh` 与 `projects/*/scripts/uat-*.sh`（D-08），不进默认 CI（D-05/D-13）。
3. **部署步骤**：`infra.sh` profiles（core/vector/mq/search/cloud）+ 三项目 override 叠加命令（从各 project README 收敛，勿复制过时端口）；`deploy-smoke.sh` 用法；端口 19100/19200/19300 与 Demo `180NN` 冲突排查。
4. **常见排障**：Milvus 冷启动、redis-stack vs 普通 redis、API Key / `env-check.sh`、端口占用、Testcontainers 本地跳过 vs CI 有 Docker。
5. **调优（教学够用）**：简短 JVM/连接池/向量 dimensions=1024 提示，勿写成运维手册。
6. **明确非目标**：无 K8s/Helm；无 Boot4/Spring AI 2.0 升级（deferred）。

更新 `docs/README.md` 总览表增加第 05 行；根 `README.md`「快速开始」或结构区增加指向生产化文档与 `quality-gate.sh` / CI 的短链（保持简洁，勿重写整份 README）。
  </action>
  <verify>
    <automated>test -f docs/00-overview/05-生产化与运维.md && grep -c 'quality-gate' docs/00-overview/05-生产化与运维.md | grep -qE '^[1-9]' && grep -c 'infra\.sh' docs/00-overview/05-生产化与运维.md | grep -qE '^[1-9]' && grep -c '05-生产化与运维' docs/README.md | grep -qE '^[1-9]' && grep -cE '05-生产化|quality-gate' README.md | grep -qE '^[1-9]'</automated>
  </verify>
  <done>生产化文档可读且含 CI/门禁/部署/排障；docs/README 与根 README 已链接。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维文档 → 学习者环境 | 文档中的演示口令/默认配置不可当作生产密钥 |
| deploy-smoke → 本地 Docker API | 仅操作本仓库 compose 文件路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | 演示默认口令 | mitigate | 文档强制「替换演示口令」警告 |
| T-07-08 | Information Disclosure | 部署文档 | mitigate | 密钥仅 env；链到 setup-env.example，不写真实 Key |
| T-07-09 | Denial of Service | smoke 过早失败 | mitigate | Milvus wait/retry，超时明确失败信息 |
| T-07-SC | Tampering | 无新包安装 | accept | 本计划仅脚本与文档 |
</threat_model>

<verification>
- `bash -n scripts/deploy-smoke.sh`
- 文档与索引 grep 门禁
- 不要求本计划内真实拉起全套中间件（耗时长）；Wave 5 再做门禁级验证
</verification>

<success_criteria>
- D-10/D-11/D-12 满足：Compose 路径文档化 + smoke 骨架 + 无 K8s
- 学习者从根 README 能跳到生产化章节并复制命令
</success_criteria>

<output>
Create `.planning/phases/07-production/07-03-SUMMARY.md` when done
</output>
