---
phase: 04-knowledge-qa-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/
  - projects/knowledge-qa-platform/src/main/java/com\/flywhl\/
autonomous: true
requirements:
  - REQ-phase-4-knowledge-qa
user_setup: []
must_haves:
  truths:
    - 全部 64 个占位 Java 类位于标准 Maven 包路径 com/flywhl/saa/knowledgeqa/**
    - KqaProperties 绑定 application.yml 的 kqa.minio/rag/memory/security.jwt 键
    - 8 张表均有 @Entity 与 JpaRepository，字段与 db/schema.sql 列名一致
    - DTO/VO record 可被 Controller 层引用编译
  artifacts:
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/KqaProperties.java
      provides: kqa.* 配置绑定
      contains: "@ConfigurationProperties"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/model/entity/SysUser.java
      provides: sys_user 实体
      contains: "@Entity"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/repository/SysUserRepository.java
      provides: 用户持久化
      contains: "JpaRepository"
  key_links:
    - from: KqaProperties
      to: application.yml
      via: "@ConfigurationProperties(prefix = \"kqa\")"
      pattern: "kqa\\.(minio|rag|memory|security)"
    - from: model/entity/*
      to: db/schema.sql
      via: "@Table/@Column 列名对齐"
      pattern: "@Table\\(name"
---

<objective>
Wave 0 路径修复 + 数据层地基：迁移畸形包目录，实现 KqaProperties、8 实体、8 Repository、4 MapStruct Mapper、全部 DTO/VO record。

Purpose: 解除 Maven/IDE 编译阻塞（D-01 棕地前提），为 config/rag/service 波次提供可注入的持久化契约（D-05、D-08）。
Output: `mvn -f projects/knowledge-qa-platform/pom.xml compile` 通过；零「骨架占位」注释；starter 自动装配 GlobalExceptionHandler（D-05，无需重复 @Import）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-knowledge-qa-platform/04-CONTEXT.md
@.planning/phases/04-knowledge-qa-platform/04-PATTERNS.md
@projects/knowledge-qa-platform/README.md
@projects/knowledge-qa-platform/db/schema.sql
@projects/knowledge-qa-platform/src/main/resources/application.yml
@starter/src/main/java/com/flywhl/saa/starter/autoconfigure/SaaLearningAutoConfiguration.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — 畸形包路径迁移</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/**, projects/knowledge-qa-platform/src/main/java/com\/flywhl\/</files>
  <action>
    将 src/main/java/com\/flywhl\/saa\/knowledgeqa/ 下全部 63 个 .java 迁移至 src/main/java/com/flywhl/saa/knowledgeqa/ 对应子包（config/controller/service/rag/admin/prompt/tool/model/mapper/repository），保持 package 声明 com.flywhl.saa.knowledgeqa.* 不变。删除字面反斜杠目录 com\/flywhl\/。KnowledgeQaApplication.java 已在正确路径，勿重复。迁移后 rg 确认无 com\\/flywhl 路径残留。本任务仅移动文件与删除占位目录，不实现业务逻辑（per D-01 路径阻塞项，04-PATTERNS 波次 0）。
  </action>
  <verify>
    <automated>find projects/knowledge-qa-platform/src/main/java -name '*.java' | wc -l | grep -q '^65$' &amp;&amp; test ! -d 'projects/knowledge-qa-platform/src/main/java/com\/flywhl\/'</automated>
  </verify>
  <done>65 个 Java 源文件均在 com/flywhl/saa/knowledgeqa/** 标准路径；畸形目录已删除</done>
</task>

<task type="auto">
  <name>Task 2: KqaProperties + 8 Entity</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/KqaProperties.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/model/entity/*.java</files>
  <action>
    实现 KqaProperties 为 @ConfigurationProperties(prefix="kqa") record，嵌套 record：Minio(endpoint/accessKey/secretKey/bucket)、Rag(topK/similarityThreshold/chunkSize/chunkOverlap/citationEnabled)、Memory(maxMessages/ttl)、Security(Jwt(issuer/accessTokenTtl))；紧凑构造器填 application.yml 默认值（per D-08、04-PATTERNS KqaProperties analog SaaLearningProperties）。实现 8 个 @Entity：SysUser、KbDocument、KbChunk、PromptTemplateEntity、QaConversation、QaMessage、QaFeedback、AuditLog——@Table/@Column 列名与 schema.sql 完全一致；TIMESTAMPTZ 用 OffsetDateTime；qa_message.citations 与 audit_log.detail 用 @JdbcTypeCode(SqlTypes.JSON)；外键关系 @ManyToOne 可选 lazy。删除全部「骨架占位」类注释，加 @author flywhl。禁止 ddl-auto 变更（per D-08）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>8 Entity 编译通过且 grep @Entity 计数为 8；KqaProperties 含 minio/rag/memory/security 四组字段</done>
</task>

<task type="auto">
  <name>Task 3: Repository + Mapper + DTO/VO</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/repository/*.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/mapper/*.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/model/dto/*.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/model/vo/*.java</files>
  <action>
    8 个 JpaRepository 接口：按 schema 索引列添加派生查询（如 KbDocumentRepository.findByStatusAndCategory Page 分页；PromptTemplateRepository.findByTemplateKeyAndStatusOrderByVersionDesc；QaConversationRepository.findByUserIdOrderByUpdatedAtDesc 等——命名由执行者 discretion per D-05）。4 个 MapStruct @Mapper(componentModel="spring")：DocumentConverter、UserConverter、PromptConverter、MessageConverter（entity↔VO）。5 DTO record + jakarta.validation：LoginRequest、QaRequest、FeedbackRequest、PromptSaveRequest、UserCreateRequest（字段对齐 README/api.http）。6 VO record：QaAnswerVO(answer+citations)、CitationVO(documentId/documentTitle/chunkId/snippet/score)、ConversationVO、DocumentVO、DashboardStatsVO、UserVO。全部删除占位注释，零 TODO（per D-01）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile &amp;&amp; rg -v '^#' projects/knowledge-qa-platform/src/main/java | rg -c '骨架占位' | grep -q '^0$'</automated>
  </verify>
  <done>Repository/Mapper/DTO/VO 编译绿；源码无「骨架占位」字符串</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码树→Maven | 畸形路径导致类不可编译/不可发现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | 包路径迁移 | mitigate | Task 1 自动化 find 计数 + 删除畸形目录 |
| T-04-SC | Tampering | MapStruct 代码生成 | accept | 父 POM 锁定 mapstruct-processor，无新增 npm 包 |
</threat_model>

<verification>
- [ ] 65 Java 文件均在标准包路径
- [ ] `mvn -f projects/knowledge-qa-platform/pom.xml compile` 成功
- [ ] rg 无「骨架占位」/ TODO
</verification>

<success_criteria>
Wave 0 + 数据层完成；后续 config 波次可注入 Repository 与 KqaProperties
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-qa-platform/04-01-SUMMARY.md` when done
</output>
