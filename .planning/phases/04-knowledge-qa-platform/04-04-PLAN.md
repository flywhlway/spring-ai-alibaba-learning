---
phase: 04-knowledge-qa-platform
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/
autonomous: true
requirements:
  - REQ-phase-4-knowledge-qa
user_setup:
  - service: dashscope
    why: 问答调用 ChatClient
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
must_haves:
  truths:
    - POST /api/auth/login 匿名签发 JWT，GET /api/auth/me 返回当前用户
    - POST /api/qa/ask 返回 Result QaAnswerVO 含 citations
    - GET /api/qa/stream SSE 事件顺序 message→meta→done，error 复用 Result
    - 会话 CRUD 删除时清 Redis ChatMemory
    - 反馈 POST /api/qa/feedback 持久化 qa_feedback
  artifacts:
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/AuthController.java
      provides: 认证 REST
      contains: "/api/auth/login"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/QaController.java
      provides: 同步+流式问答
      contains: "text/event-stream"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/QaService.java
      provides: 问答核心
      contains: "CitationPostProcessor"
  key_links:
    - from: QaService
      to: ChatClient
      via: advisors param CONVERSATION_ID
      pattern: "ChatMemory\\.CONVERSATION_ID"
    - from: QaController stream
      to: QaService
      via: Flux ServerSentEvent message/meta/done
      pattern: "event\\(\"meta\"\\)"
---

<objective>
波次 3 问答域：Auth、Qa（同步+SSE）、Conversation、Feedback 的 Controller + Service 全量实现。

Purpose: 交付 D-13~D-15 Citation/SSE 协议、D-20 会话记忆、README §6 接口契约；对齐 http/api.http 路径与权限。
Output: 员工域 API 可 curl 验收（需 infra + API Key）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-knowledge-qa-platform/04-CONTEXT.md
@.planning/phases/04-knowledge-qa-platform/04-PATTERNS.md
@projects/knowledge-qa-platform/README.md
@projects/knowledge-qa-platform/http/api.http
@examples/44-stream-demo/src/main/java/com/flywhl/saa/stream/StreamController.java
@examples/28-advanced-rag-demo/src/main/java/com/flywhl/saa/advancedrag/AdvancedRagController.java
@examples/17-redis-memory-demo/src/main/java/com/flywhl/saa/redismemory/MemoryController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthService + AuthController</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/AuthService.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/AuthController.java</files>
  <action>
    AuthService：login(LoginRequest) 校验 SysUser enabled，JwtEncoder 签发 accessToken（claims: sub/username/role，ttl kqa.security.jwt.access-token-ttl）；返回 VO 含 accessToken+用户信息。me() 从 SecurityContext 取当前用户转 UserVO。AuthController：POST /api/auth/login permitAll @Valid LoginRequest → Result；GET /api/auth/me authenticated → Result UserVO。失败抛 BizException→GlobalExceptionHandler。新建用户 BCrypt 逻辑留 admin 波次（per D-24 演示账号仅 data.sql {noop}）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>认证接口编译通过；JWT 使用 Nimbus 非 jjwt</done>
</task>

<task type="auto">
  <name>Task 2: QaService + QaController（同步 + SSE）</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/QaService.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/QaController.java</files>
  <action>
    QaService（per D-13~D-15）：ask(QaRequest) 调用 chatClient.prompt().advisors(a→a.param(ChatMemory.CONVERSATION_ID, conversationId)).user(question).call()；同步路径用 RagPipelineFactory.documentRetriever().retrieve() 或 Advisor 上下文取 Document 列表→CitationPostProcessor→QaAnswerVO(answer,citations)；持久化 qa_message+qa_conversation。stream(conversationId,question) 返回 Flux ServerSentEvent：message 增量文本（44-stream 模式）→ 流结束后 meta JSON（citations+token usage 从 ChatResponse metadata/Micrometer）→ done；onErrorResume error 事件 payload Result.fail。正文不内联 [1] 脚注。QaController：POST /api/qa/ask @PreAuthorize EMPLOYEE+；GET /api/qa/stream produces TEXT_EVENT_STREAM。SSE 实现 SseEmitter 或 Flux 均可（discretion D-05）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>问答同步+流式接口编译通过；SSE 事件名含 message/meta/done/error</done>
</task>

<task type="auto">
  <name>Task 3: Conversation + Feedback 域</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/ConversationService.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/service/FeedbackService.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/ConversationController.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/controller/FeedbackController.java</files>
  <action>
    ConversationService：分页列表 Result PageResult ConversationVO；getMessages(conversationId) 历史消息；delete(conversationId) 清 RedisChatMemoryRepository + 更新/软删 qa_conversation（对齐 README DELETE 语义）。ConversationController：GET /api/conversations、GET /api/conversations/{cid}/messages、DELETE /api/conversations/{cid}，@PreAuthorize EMPLOYEE+。FeedbackService：save(FeedbackRequest) 写 qa_feedback 关联 messageId。FeedbackController：POST /api/qa/feedback。全部返回 Result/Result PageResult 包装（common 模式）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>会话与反馈四接口实现完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/qa/* | JWT + EMPLOYEE 角色 |
| QaService→ChatClient | 用户 question 注入 LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | /api/qa/* | mitigate | @PreAuthorize hasRole EMPLOYEE |
| T-04-08 | Tampering | conversationId | mitigate | 校验会话归属当前 userId |
| T-04-09 | Repudiation | qa_message | mitigate | 持久化问答记录供审计 |
</threat_model>

<verification>
- [ ] `mvn -f projects/knowledge-qa-platform/pom.xml compile` 成功
- [ ] Controller 路径与 api.http 一致（rg @RequestMapping/@GetMapping/@PostMapping）
</verification>

<success_criteria>
员工域 REST+SSE 全实现；可进入 admin 波次
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-qa-platform/04-04-SUMMARY.md` when done
</output>
