---
phase: 04-knowledge-qa-platform
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/AiClientConfig.java
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/DemoKnowledgeSeeder.java
autonomous: true
requirements:
  - REQ-phase-4-knowledge-qa
user_setup:
  - service: milvus
    why: ETL 与 RAG 向量写入
    env_vars: []
must_haves:
  truths:
    - RagPipelineFactory 产出 RetrievalAugmentationAdvisor，top-k=5 similarity-threshold=0.35 allowEmptyContext=false
    - DocumentEtlPipeline 异步状态机 UPLOADED→PARSING→INDEXED|FAILED
    - CitationPostProcessor 从 Document metadata 组装 CitationVO
    - AiClientConfig Advisor 链：MessageChatMemoryAdvisor → RAG → AuditLoggingAdvisor，ChatModel 经 FallbackModelRouter
    - 演示文档 INDEXED 且无 chunk 时 Seeder 触发 reindex 写入 Milvus
  artifacts:
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/RagPipelineFactory.java
      provides: Modular RAG Advisor
      contains: "RetrievalAugmentationAdvisor"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/DocumentEtlPipeline.java
      provides: 异步 ETL
      contains: "@Async"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/AiClientConfig.java
      provides: ChatClient Bean
      contains: "defaultAdvisors"
  key_links:
    - from: DocumentEtlPipeline
      to: VectorStore
      via: vectorStore.add metadata documentId/chunkId/title
      pattern: "vectorStore\\.add"
    - from: AiClientConfig
      to: FallbackModelRouter
      via: ChatClient.builder(modelRouter.route())
      pattern: "modelRouter\\.route"
---

<objective>
波次 2 rag/* + AiClientConfig 完整装配 + DemoKnowledgeSeeder：Modular RAG、异步 ETL、Citation 后处理、入库状态跟踪、多模型 Advisor 链。

Purpose: 交付 D-09~D-12 RAG、D-16~D-18 ETL、D-19 ChatClient 链；解决 data.sql INDEXED 无向量 UAT 阻塞（04-RESEARCH A2）。
Output: RAG/ETL Bean 可注入；Seeder 可在启动后补齐演示向量。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-knowledge-qa-platform/04-CONTEXT.md
@.planning/phases/04-knowledge-qa-platform/04-PATTERNS.md
@examples/28-advanced-rag-demo/src/main/java/com/flywhl/saa/advancedrag/AdvancedRagConfig.java
@examples/27-rag-demo/src/main/java/com/flywhl/saa/rag/RagController.java
@examples/47-routing-demo/src/main/java/com/flywhl/saa/routing/RoutingController.java
@examples/44-stream-demo/src/main/java/com/flywhl/saa/stream/StreamConfig.java
@projects/knowledge-qa-platform/db/data.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: RagPipelineFactory + CitationPostProcessor</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/RagPipelineFactory.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/CitationPostProcessor.java</files>
  <action>
    RagPipelineFactory @Component（per D-09、D-10、04-PATTERNS）：RewriteQueryTransformer.builder().chatClientBuilder(chatClientBuilder.build().mutate())——必须 mutate 防 RAG 递归；VectorStoreDocumentRetriever similarityThreshold/topK 读 KqaProperties.rag；DocumentPostProcessor 按 score 降序 limit(topK)；RetrievalAugmentationAdvisor allowEmptyContext(false)。暴露 Bean retrievalAugmentationAdvisor() 与 documentRetriever() 单例供 Citation 复用（禁止双检索不一致，04-RESEARCH Pitfall 1）。CitationPostProcessor：输入 List Document 检索结果，映射 CitationVO(documentId/documentTitle/chunkId/snippet/score) 从 metadata documentId/chunkId/title + doc text 截断；答案正文不内联脚注（per D-15）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>RAG Factory 与 Citation 处理器编译通过；无 QuestionAnswerAdvisor</done>
</task>

<task type="auto">
  <name>Task 2: DocumentEtlPipeline + IngestStatusTracker</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/DocumentEtlPipeline.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/rag/IngestStatusTracker.java</files>
  <action>
    IngestStatusTracker @Service：封装 kb_document.status 更新与 fail_reason 写入；提供 markParsing/markIndexed/markFailed/getStatus。DocumentEtlPipeline @Service（per D-16~D-18）：方法 ingestAsync(documentId) @Async("etlExecutor")；流程 MinIO 下载 → Tika DocumentReader → TokenTextSplitter chunk-size/overlap 读 kqa.rag（1.1.2 无 overlap API 时用 sliding window 或相邻块重叠策略 per RESEARCH Assumptions）→ 每 chunk vectorStore.add(metadata: documentId/chunkId/title/seqNo) + kb_chunk 批量 save（milvus_pk=Document.getId()）；成功 INDEXED+chunk_count；失败 FAILED+fail_reason。deleteIndex(documentId) 级联删 Milvus 向量（按 metadata documentId filter 或存 pk 列表）+ kb_chunk。reindex 先 delete 再 ingestAsync。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>ETL 流水线与状态跟踪器可编译；状态枚举对齐 UPLOADED/PARSING/INDEXED/FAILED</done>
</task>

<task type="auto">
  <name>Task 3: AiClientConfig + DemoKnowledgeSeeder</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/AiClientConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/DemoKnowledgeSeeder.java</files>
  <action>
    AiClientConfig @Configuration（per D-19、D-06、D-07）：ChatModel model = fallbackModelRouter.route()；ChatClient.builder(model).defaultSystem(promptTemplateProvider.get("qa-system")).defaultAdvisors(messageChatMemoryAdvisor, retrievalAugmentationAdvisor, auditLoggingAdvisor).build()——Advisor 顺序 Memory 在 RAG 前（04-RESEARCH Pitfall 4）。禁用 PromptChatMemoryAdvisor；Options 一律 Builder。DemoKnowledgeSeeder @Component ApplicationRunner：启动后查 kb_document status=INDEXED 且 chunk_count=0（或 kb_chunk 空），对每条调用 documentEtlPipeline.reindex——确保 data.sql 预置文档有 Milvus 向量供 UAT（per D-31、04-RESEARCH A2）。若 MinIO 缺演示文件，Seeder 写内联 sample 文本 Document 入库而非失败静默。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>ChatClient Bean 定义完成；Seeder 存在且调用 reindex/ingest</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ETL→MinIO | 文件下载，对象键来自 DB |
| ETL→Milvus | 向量写入，metadata 含 documentId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | MinIO object key | mitigate | 仅使用 kb_document.minio_object DB 字段，忽略用户输入路径 |
| T-04-06 | Information Disclosure | ETL fail_reason | accept | 管理员可见，不返回给员工 API |
</threat_model>

<verification>
- [ ] `mvn -f projects/knowledge-qa-platform/pom.xml compile` 成功
- [ ] rg 无 QuestionAnswerAdvisor/PromptChatMemoryAdvisor
</verification>

<success_criteria>
RAG + ETL + ChatClient 装配完成；演示知识可被 Seeder 索引
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-qa-platform/04-03-SUMMARY.md` when done
</output>
