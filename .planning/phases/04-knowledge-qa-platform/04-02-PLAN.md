---
phase: 04-knowledge-qa-platform
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/
  - projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/prompt/PromptTemplateProvider.java
autonomous: true
requirements:
  - REQ-phase-4-knowledge-qa
user_setup:
  - service: dashscope
    why: AiClientConfig 装配需 ChatModel Bean（运行时）
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
  - service: deepseek
    why: FallbackModelRouter 备用通道
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: scripts/setup-env.sh
must_haves:
  truths:
    - Spring Security JWT Resource Server 保护 /api/**，/api/auth/login 与 /actuator/health 匿名
    - MinIO Client Bean 可连 kqa-documents bucket
    - Redis ChatMemory max-messages=20 配置生效
    - PromptTemplateProvider 三级回退可读 qa-system.st
  artifacts:
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/SecurityConfig.java
      provides: JWT 鉴权链
      contains: "oauth2ResourceServer"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/ChatMemoryConfig.java
      provides: Redis 会话记忆
      contains: "MessageWindowChatMemory"
    - path: projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/prompt/PromptTemplateProvider.java
      provides: Prompt 读取门面
      contains: "getTemplate"
  key_links:
    - from: SecurityConfig
      to: SysUserRepository
      via: UserDetailsService 加载演示账号
      pattern: "UserDetailsService"
    - from: ChatMemoryConfig
      to: kqa.memory.*
      via: KqaProperties
      pattern: "maxMessages|max-messages"
---

<objective>
波次 1 基础设施 config/*：Security JWT、MinIO、VectorStore、Redis Memory、Async、OpenAPI、PromptTemplateProvider（不含 RAG ChatClient 完整链——留 04-03 与 RagPipelineFactory 汇合）。

Purpose: 按 D-04 波次 1 交付中间件 Bean 图；D-23 JWT、D-20 Redis Memory、D-21 Prompt 读取、D-07 禁用废弃 API。
Output: 应用可启动（RAG Advisor 未接时 Qa 暂不可用属预期）；compile 门禁通过。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-knowledge-qa-platform/04-CONTEXT.md
@.planning/phases/04-knowledge-qa-platform/04-PATTERNS.md
@projects/knowledge-qa-platform/src/main/resources/application.yml
@examples/17-redis-memory-demo/src/main/java/com/flywhl/saa/redismemory/MemoryConfig.java
@examples/24-milvus-demo/src/main/java/com/flywhl/saa/milvus/MilvusController.java
@examples/08-prompt-nacos-demo/src/main/java/com/flywhl/saa/promptnacos/PromptNacosController.java
@docs/00-overview/ADR-006-api-documentation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SecurityConfig + JWT + AsyncConfig</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/SecurityConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/AsyncConfig.java</files>
  <action>
    SecurityConfig（per D-23、D-24）：@EnableWebSecurity @EnableMethodSecurity；OAuth2 Resource Server jwt()；SecurityFilterChain 规则——permitAll: POST /api/auth/login, GET /actuator/health；authenticated: 其余 /api/**；@PreAuthorize 角色 ADMIN/EMPLOYEE 留给 Controller。JwtEncoder/JwtDecoder 用 NimbusJwtEncoder/NimbusJwtDecoder + 对称密钥（从 kqa.security.jwt 或 spring.security.oauth2.resourceserver.jwt 读取，禁止 jjwt）。UserDetailsService 注入 SysUserRepository，DelegatingPasswordEncoder 兼容 {noop} 与 BCrypt。AsyncConfig：@EnableAsync，Bean etlExecutor（ThreadPoolTaskExecutor，core=2 max=4 queue=100，线程名 prefix kqa-etl-）供 DocumentEtlPipeline 使用（per D-16）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>Security 与 Async 配置类编译通过；无 CallAroundAdvisor/PromptChatMemoryAdvisor</done>
</task>

<task type="auto">
  <name>Task 2: Minio + VectorStore + ChatMemory + OpenAPI</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/MinioConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/VectorStoreConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/ChatMemoryConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/OpenApiConfig.java, projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/config/RedisChatMemoryRepository.java</files>
  <action>
    MinioConfig：@ConfigurationProperties 绑定 kqa.minio，MinioClient Bean（endpoint/accessKey/secretKey）。VectorStoreConfig：依赖 Spring AI 自动配置 Milvus collection kqa_knowledge embedding-dimension 1024；可选 @PostConstruct 日志确认 collection；暴露 SearchRequest 默认参数读取 kqa.rag.*（per D-09、D-12）。ChatMemoryConfig（per D-20、04-PATTERNS）：复制 17-redis-memory 模式，MessageWindowChatMemory maxMessages 来自 kqa.memory.max-messages(20)；实现 RedisChatMemoryRepository（List 存消息，key 前缀 kqa:chat-memory:，TTL 绑定 kqa.memory.ttl 7d）。**同时暴露 `@Bean MessageChatMemoryAdvisor messageChatMemoryAdvisor(ChatMemory chatMemory)`**（`MessageChatMemoryAdvisor.builder(chatMemory).build()`，对齐 `17-redis-memory-demo/MemoryConfig.java`），供 04-03 AiClientConfig 注入。OpenApiConfig：GroupedOpenApi 分组 auth/qa/admin，Knife4j 中文，doc.html 可访问（ADR-006）。本任务不在 ChatMemoryConfig 内创建带 RAG 的 ChatClient——避免与 04-03 RagPipelineFactory 循环依赖。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>MinIO/VectorStore/Redis Memory/OpenAPI 四类 Config 就绪</done>
</task>

<task type="auto">
  <name>Task 3: PromptTemplateProvider</name>
  <files>projects/knowledge-qa-platform/src/main/java/com/flywhl/saa/knowledgeqa/prompt/PromptTemplateProvider.java</files>
  <action>
    实现 PromptTemplateProvider @Component（per D-21、D-22）：三级回退 Nacos ConfigurablePromptTemplateFactory.getTemplate(key) → DB prompt_template status=PUBLISHED 最新 version → classpath prompts/{key}.st（qa-system.st、query-rewrite.st 已存在）。公开方法 get(String templateKey) 返回 String 内容；getQueryRewriteTemplate() 供 RagPipelineFactory 使用。Nacos 不可用时静默降级 DB/classpath，不阻塞启动。PromptPublishService 留 04-05 admin 波次。删除占位注释。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q compile</automated>
  </verify>
  <done>PromptTemplateProvider 可读 classpath qa-system.st；compile 绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/auth/login | 匿名登录，凭据校验 |
| client→/api/** | JWT 校验，RBAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Spoofing | Auth login | mitigate | DelegatingPasswordEncoder + JWT issuer/ttl 绑定 kqa.security.jwt |
| T-04-03 | Information Disclosure | JWT secret | mitigate | 密钥仅环境变量/YAML 占位，禁止硬编码 sk- |
| T-04-04 | Denial of Service | etlExecutor | mitigate | 有界队列 + maxPoolSize=4 |
</threat_model>

<verification>
- [ ] `mvn -f projects/knowledge-qa-platform/pom.xml compile` 成功
- [ ] rg 项目源码无 PromptChatMemoryAdvisor/FunctionCallback
</verification>

<success_criteria>
config 基础设施 Bean 就绪；04-03 可装配 RAG 与 AiClientConfig
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-qa-platform/04-02-SUMMARY.md` when done
</output>
