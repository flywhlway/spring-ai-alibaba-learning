---
phase: 04-knowledge-qa-platform
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - projects/knowledge-qa-platform/pom.xml
  - projects/knowledge-qa-platform/src/test/
  - scripts/uat-knowledge-qa.sh
  - .planning/phases/04-knowledge-qa-platform/04-UAT.md
autonomous: true
requirements:
  - REQ-phase-4-knowledge-qa
user_setup:
  - service: dashscope
    why: 真实模型 IT
    env_vars:
      - name: AI_DASHSCOPE_API_KEY
        source: scripts/setup-env.sh
  - service: docker
    why: Testcontainers PostgreSQL/Redis
    env_vars: []
must_haves:
  truths:
    - 无 API Key 时 mvn clean install 全绿
    - 有 API Key 时 login/ask/stream 各至少 1 IT 通过
    - version-audit 与 spring-ai-2-readiness 全绿
    - 04-UAT.md 覆盖 api.http 全接口 curl 预期
  artifacts:
    - path: projects/knowledge-qa-platform/src/test/java/com/flywhl/saa/knowledgeqa/KnowledgeQaApplicationTests.java
      provides: Context 加载 IT
      contains: "@SpringBootTest"
    - path: .planning/phases/04-knowledge-qa-platform/04-UAT.md
      provides: curl 验收清单
      contains: "api/auth/login"
  key_links:
    - from: AuthIntegrationTest
      to: /api/auth/login
      via: MockMvc/TestRestTemplate
      pattern: "api/auth/login"
---

<objective>
波次 5 测试 + HANDOFF §7 质量门禁 + 04-UAT.md 全接口 curl 验收清单。

Purpose: 交付 D-28~D-31 Testcontainers/API Key IT、HANDOFF §7、README curl 闭环。
Output: `mvn -f projects/knowledge-qa-platform/pom.xml clean install` 无 Key 全绿；04-UAT.md 可执行验收。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-knowledge-qa-platform/04-CONTEXT.md
@HANDOFF-TO-CLAUDE-CODE.md
@projects/knowledge-qa-platform/http/api.http
@examples/44-stream-demo/src/test/java/com/flywhl/saa/stream/StreamDemoApplicationIT.java
@starter/src/test/java/com/flywhl/saa/starter/routing/FallbackModelRouterTest.java
@.claude/skills/saa-conventions/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 单元测试 + pom Testcontainers</name>
  <files>projects/knowledge-qa-platform/pom.xml, projects/knowledge-qa-platform/src/test/java/com/flywhl/saa/knowledgeqa/</files>
  <action>
    pom.xml 增加 test scope：org.testcontainers:junit-jupiter、postgresql、redis（版本 Boot BOM，per 04-RESEARCH Package Audit）。单元测试（优先核心路径 per D-28 discretion）：CitationPostProcessorTest（metadata→CitationVO）、RagPipelineFactoryTest（mock VectorStore 验证 postProcessor limit）、AuthServiceTest（mock JwtEncoder/UserRepo）、DocumentEtlPipelineTest（mock MinIO/VectorStore 状态转换）。JUnit5+AssertJ+Mockito；@DisplayName 中文。KnowledgeQaApplicationTests 改为 @SpringBootTest 空上下文加载（Testcontainers 动态属性可 stub 或 @MockBean 外部中间件使无 Docker 也能绿——仅验证 ApplicationContext 启动）。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q test -Dtest=CitationPostProcessorTest,AuthServiceTest,RagPipelineFactoryTest,KnowledgeQaApplicationTests</automated>
  </verify>
  <done>核心单测 + 冒烟测试无 Key 可绿</done>
</task>

<task type="auto">
  <name>Task 2: Testcontainers IT + API Key 门控 IT</name>
  <files>projects/knowledge-qa-platform/src/test/java/com/flywhl/saa/knowledgeqa/</files>
  <action>
    KnowledgeQaPostgresRedisIT：@Testcontainers PostgreSQLContainer postgres:16 + GenericContainer redis:7；@DynamicPropertySource 覆盖 datasource/redis；验证 Repository 或 Auth login MockMvc（无需 Milvus，per D-28 Milvus IT 可选）。AuthIntegrationTest：POST /api/auth/login admin 返回 code=0+token（无 API Key）。QaAskIT、QaStreamIT：@EnabledIfEnvironmentVariable(named=AI_DASHSCOPE_API_KEY, matches=.+)；@SpringBootTest webEnvironment=RANDOM_PORT；需 Milvus 运行时 infra 或 @Disabled 文档说明手动 profile（不阻塞无 Key CI，per D-29）。QaStreamIT 断言 SSE 含 event message 与 done。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q test</automated>
  </verify>
  <done>无 Key mvn test 全绿；有 Key 时 ask/stream IT 可运行</done>
</task>

<task type="auto">
  <name>Task 3: HANDOFF §7 门禁 + smoke 脚本 + 04-UAT.md</name>
  <files>scripts/uat-knowledge-qa.sh, .planning/phases/04-knowledge-qa-platform/04-UAT.md</files>
  <action>
    执行 HANDOFF §7（per D-30）：mvn -f projects/knowledge-qa-platform/pom.xml clean install；bash scripts/version-audit.sh；bash scripts/spring-ai-2-readiness.sh .；rg 确认无废弃 API/硬编码密钥/TODO/骨架占位。新增 **scripts/uat-knowledge-qa.sh**：前置检查 infra（PG/Redis/Milvus/MinIO healthy）→ 后台 `mvn -f projects/knowledge-qa-platform/pom.xml spring-boot:run` → 等待 19100 health → 执行核心 curl（login 取 token、zhangsan ask 断言 citations 非空、stream 断言 message/meta/done 事件、employee 访问 admin 403）→ 清理进程；无 `AI_DASHSCOPE_API_KEY` 时跳过 ask/stream 仅验 login/health。编写 04-UAT.md：按 api.http 逐接口列出 curl、Authorization 头、预期 code/字段；含演示账号 zhangsan 问「出差住宿费」闭环；注明 Milvus 冷启动与 `docker compose ... --profile kqa up -d` 前置。修复门禁失败项直至全绿。
  </action>
  <verify>
    <automated>mvn -f projects/knowledge-qa-platform/pom.xml -q clean install &amp;&amp; bash scripts/version-audit.sh &amp;&amp; bash scripts/spring-ai-2-readiness.sh . &amp;&amp; test -x scripts/uat-knowledge-qa.sh</automated>
    <manual>有 Key+infra 时：bash scripts/uat-knowledge-qa.sh 全绿</manual>
  </verify>
  <done>clean install + 两脚本全绿；uat-knowledge-qa.sh 可执行；04-UAT.md 覆盖 api.http</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI→Testcontainers | 本地 Docker 拉起 PG/Redis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | IT 日志 | accept | 测试不用生产密钥 |
| T-04-SC | Tampering | testcontainers 依赖 | mitigate | Maven Central BOM 版本，Package Audit 已 Approved |
</threat_model>

<verification>
- [ ] `mvn -f projects/knowledge-qa-platform/pom.xml clean install` 无 Key 成功
- [ ] version-audit.sh 全绿
- [ ] spring-ai-2-readiness.sh 低位
- [ ] 04-UAT.md 完整
</verification>

<success_criteria>
Phase 4 HANDOFF §7 收口；UAT 清单就绪供 /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-qa-platform/04-06-SUMMARY.md` when done
</output>
