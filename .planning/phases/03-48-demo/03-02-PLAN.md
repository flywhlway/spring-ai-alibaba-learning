---
phase: 03-48-demo
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - examples/05-retry-demo/
  - examples/08-prompt-nacos-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 08 真机 curl 需 `bash scripts/infra.sh up cloud`（Nacos）；编译不依赖 Nacos
must_haves:
  truths:
    - 05-retry-demo 演示 spring.ai.retry.* 与自定义 RetryTemplate
    - 08-prompt-nacos-demo 演示 ConfigurablePromptTemplateFactory 与默认模板兜底
    - 首批 01~08 全部 compile 通过
  artifacts:
    - examples/05-retry-demo/pom.xml
    - examples/05-retry-demo/README.md
    - examples/05-retry-demo/api.http
    - examples/08-prompt-nacos-demo/pom.xml
    - examples/08-prompt-nacos-demo/README.md
    - examples/08-prompt-nacos-demo/api.http
  key_links:
    - 05 → docs/tutorial/04-ChatClient.md §4.8
    - 08 → docs/tutorial/05-Prompt.md 可运行 Demo
---

<objective>
新建缺失的 05-retry-demo 与空壳 08-prompt-nacos-demo，并完成首批 01~08 编译门禁。

Purpose: 补齐 Phase 3 首批缺口，使 01~08 均可独立编译运行。
Output: 两个完整 Demo + 冒烟 IT + 全批 compile 绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@docs/tutorial/04-ChatClient.md
@docs/tutorial/05-Prompt.md
@examples/04-chat-demo/
@examples/06-prompt-demo/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 05-retry-demo</name>
  <files>examples/05-retry-demo/</files>
  <read_first>docs/tutorial/04-ChatClient.md, examples/04-chat-demo/pom.xml, examples/04-chat-demo/src/main/java/com/flywhl/saa/chat/ChatDemoApplication.java</read_first>
  <action>
创建 `examples/05-retry-demo/`，端口 18005，包 `com.flywhl.saa.retry`：
- pom：web + validation + dashscope + saa-learning-common（零版本号）
- application.yml：`spring.ai.retry.max-attempts=3` 与 backoff（initial 1000ms, multiplier 2.0, max 10000ms）；密钥 `${AI_DASHSCOPE_API_KEY}`
- `RetryConfig`：自定义 `RetryTemplate` Bean（`@ConditionalOnMissingBean` 不适用时直接声明覆盖），带 `RetryListener` 记录 attempt 次数到内存计数器
- `RetryController`：`GET /chat`（正常问答，`Result&lt;String&gt;`）、`GET /retry/policy`（返回 maxAttempts/backoff 配置摘要）、`GET /retry/stats`（返回 listener 累计 attempts）
- `@Import(GlobalExceptionHandler.class)`
- README + api.http + 一个 `@EnabledIfEnvironmentVariable` 冒烟 IT（contextLoads 或简单 chat）
严格无 TODO、无硬编码密钥、无废弃 API。
  </action>
  <verify>mvn -f examples/05-retry-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - application.yml 含 spring.ai.retry.max-attempts
    - 存在自定义 RetryTemplate Bean
    - server.port=18005
  </acceptance_criteria>
  <done>05-retry-demo 可编译，规格对齐 §4.8</done>
</task>

<task type="auto">
  <name>Task 2: 实现 08-prompt-nacos-demo</name>
  <files>examples/08-prompt-nacos-demo/</files>
  <read_first>docs/tutorial/05-Prompt.md, examples/06-prompt-demo/pom.xml</read_first>
  <action>
填满空壳 `examples/08-prompt-nacos-demo/`，端口 18008，包 `com.flywhl.saa.promptnacos`：
- pom：web + dashscope + `spring-ai-alibaba-starter-nacos-prompt` + saa-learning-common
- application.yml：按教程 §可运行 Demo（nacos server-addr 127.0.0.1:8848, username/password nacos）
- `PromptNacosController`：教程代码 + 返回 `Result&lt;String&gt;`；`getTemplate("dtc-diagnosis")` 为 null 时 `create` 默认模板兜底
- Application + `@Import(GlobalExceptionHandler.class)`
- README（声明 `bash scripts/infra.sh up cloud`）+ api.http + `@EnabledIfEnvironmentVariable` 冒烟 IT
  </action>
  <verify>mvn -f examples/08-prompt-nacos-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - 依赖 spring-ai-alibaba-starter-nacos-prompt
    - 含默认模板兜底逻辑
    - server.port=18008
  </acceptance_criteria>
  <done>08-prompt-nacos-demo 可编译，规格对齐教程</done>
</task>

<task type="auto">
  <name>Task 3: 首批 01~08 编译门禁</name>
  <files>examples/0{1..8}-*/</files>
  <read_first>.planning/phases/03-48-demo/03-CONTEXT.md</read_first>
  <action>
对 01~08 每个 Demo 执行 `mvn -f examples/NN-xxx/pom.xml -q compile`。
扫描：无 TODO、无 PromptChatMemoryAdvisor/CallAroundAdvisor/FunctionCallback、无硬编码密钥。
更新 examples/README.md 若需标注首批状态（可选，非必须）。
  </action>
  <verify>八个 Demo compile 全绿</verify>
  <acceptance_criteria>
    - 01~08 均存在 pom.xml / README.md / api.http
    - 全部 compile 退出码 0
  </acceptance_criteria>
  <done>Phase 3 首批编译门禁通过</done>
</task>

</tasks>

<verification>
- [ ] 05 与 08 compile 成功
- [ ] 01~08 全绿
- [ ] 无废弃 API / 硬编码密钥 / TODO
</verification>

<success_criteria>
- 首批 8 个 Demo 工程完整且可编译
- SUMMARY 记录续接与新建内容
</success_criteria>
