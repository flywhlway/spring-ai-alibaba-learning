---
phase: 03-48-demo
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/38-workflow-demo/
  - examples/39-graph-parallel-demo/
  - examples/40-graph-saga-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 真机 curl / 冒烟 IT 需 AI_DASHSCOPE_API_KEY；编译不依赖 Key
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 35~48"
    - "D-03: 38~40 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-05: 端口 18038/18039/18040"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 Advisor/FunctionCallback/可变 Options setter"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-08b: 小 DTO/record 同包"
    - "D-09: common + Result + GlobalExceptionHandler"
    - "D-12: 规格以 docs/tutorial/14-Workflow.md 为准"
    - "D-13: 38/39/40 → 教程第 14 章"
    - "D-14: KeyStrategy.REPLACE / addEdge(List) 等 JAR 映射，禁止 addAggregatedEdge"
    - "D-15/D-16: 38~40 README 写无中间件（MemorySaver 进程内）"
    - "D-18: 38 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-20: Graph/Workflow 能力域 plan（38~40）"
    - 38 线性 StateGraph：START→rewrite→retrieve→generate→END + MemorySaver
    - 39 并行边 addEdge(START, List.of(...)) + addEdge(List.of(...), merge)，无 addAggregatedEdge
    - 40 最小 Saga：扣库存→扣款→条件边→补偿节点；forceFail 触发补偿；无外部事务中间件
    - 三 Demo 均 compile，仅声明 agent-framework（不显式钉 graph-core 版本）
  artifacts:
    - examples/38-workflow-demo/pom.xml
    - examples/38-workflow-demo/README.md
    - examples/38-workflow-demo/api.http
    - examples/39-graph-parallel-demo/pom.xml
    - examples/39-graph-parallel-demo/README.md
    - examples/39-graph-parallel-demo/api.http
    - examples/40-graph-saga-demo/pom.xml
    - examples/40-graph-saga-demo/README.md
    - examples/40-graph-saga-demo/api.http
  key_links:
    - 38/39/40 → docs/tutorial/14-Workflow.md
    - API 真源 → 03-RESEARCH.md Pattern 3 / Saga 片段
    - D-18 38 冒烟 IT
---

<objective>
新建 38-workflow-demo、39-graph-parallel-demo、40-graph-saga-demo，交付 Graph / Workflow 能力域。

Purpose: Batch 3 Wave 1 Graph 切片；线性 / 并行 / Saga 三种编排形态。
Output: 三个可编译 Demo；38 含冒烟 IT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@.planning/phases/03-48-demo/03-PATTERNS.md
@.claude/skills/saa-conventions/SKILL.md
@docs/tutorial/14-Workflow.md
@examples/04-chat-demo/
@examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
</context>

<threat_model>
## Trust Boundaries
- 用户查询参数 → StateGraph 节点 →（可选）ChatModel → REST JSON

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-38-01 | MED | 图状态键污染 / 注入 | KeyStrategy.REPLACE 明确键集合；节点仅读写声明键；Result 包装输出 |
| T-40-01 | LOW | Saga 补偿逻辑误触发资金副作用 | Demo 用内存 Map 模拟库存/扣款，无真实支付；forceFail 仅演示路径 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 38-workflow-demo（线性 StateGraph + 冒烟 IT）</name>
  <files>examples/38-workflow-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（StateGraph 映射；CompileConfig/MemorySaver）
    - docs/tutorial/14-Workflow.md（基础图 / 可运行 Demo）
    - examples/04-chat-demo/pom.xml
    - examples/04-chat-demo/src/main/java/com/flywhl/saa/chat/ChatDemoApplication.java
    - examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
    - .claude/skills/saa-conventions/SKILL.md
  </read_first>
  <action>
新建 `examples/38-workflow-demo/`（端口 18038，包 `com.flywhl.saa.workflow`，per D-03/D-05/D-09/D-13）：
1. pom：parent `relativePath=../../pom.xml`；`spring-boot-starter-web` + `validation` + `spring-ai-alibaba-starter-dashscope` + `spring-ai-alibaba-agent-framework` + `saa-learning-common` + test；**只声明 agent-framework**（graph-core 传递，勿显式钉版本，RESEARCH Anti-Patterns）
2. `server.port=18038`；`${AI_DASHSCOPE_API_KEY}`
3. 线性图（与 39 并行图区分）：`new StateGraph("workflow-demo", (KeyStrategyFactory) () -> Map.of(...))`，键用 `KeyStrategy.REPLACE`（**禁止** `KeyStrategy.replace()`）
4. 节点：rewrite → retrieve → generate（retrieve 可用内存假数据；generate 可调 ChatModel 或模板拼接，保证可 compile 且有文本输出）
5. 边：`START → rewrite → retrieve → generate → END`
6. Checkpoint：`MemorySaver` + `CompileConfig.builder().saverConfig(...)` 或等价 JAR API（查 SaverConfig）；进程内，README 写无中间件（D-15/D-16）
7. Controller：`GET /workflow/run?question=`，`compiledGraph.invoke(...)` → 从 OverAllState 取 answer → `Result.ok`
8. `@Import(GlobalExceptionHandler.class)`；README + api.http
9. 冒烟 IT（D-18）：`WorkflowDemoApplicationIT`，`@EnabledIfEnvironmentVariable(named="AI_DASHSCOPE_API_KEY", matches=".+")`，调用图或 REST 逻辑断言 answer 非空
禁止：`addAggregatedEdge`、废弃 Advisor、硬编码密钥、TODO、starter
  </action>
  <verify>
    <automated>mvn -f examples/38-workflow-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - pom 含 spring-ai-alibaba-agent-framework，无显式 spring-ai-alibaba-graph-core 版本声明，无 saa-learning-starter
    - application.yml 含 server.port: 18038
    - 源码含 StateGraph、KeyStrategy.REPLACE、MemorySaver
    - 源码不含 addAggregatedEdge、KeyStrategy.replace()
    - WorkflowDemoApplicationIT 含 @EnabledIfEnvironmentVariable(named = "AI_DASHSCOPE_API_KEY"
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>38 compile 绿，线性图 + 冒烟 IT</done>
</task>

<task type="auto">
  <name>Task 2: 新建 39-graph-parallel-demo（并行 fan-out/fan-in）</name>
  <files>examples/39-graph-parallel-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Pattern 3 并行 Graph）
    - docs/tutorial/14-Workflow.md（并行诊断完整代码段落）
    - examples/38-workflow-demo/（Task 1 骨架）
  </read_first>
  <action>
新建 `examples/39-graph-parallel-demo/`（端口 18039，包 `com.flywhl.saa.graphparallel`，per D-14）：
1. pom 同 38
2. 按 RESEARCH Pattern 3：
   - `addNode("searchKb", ...)`、`addNode("searchHistory", ...)`、`addNode("generateAnswer", ...)`
   - `addEdge(StateGraph.START, List.of("searchKb", "searchHistory"))`
   - `addEdge(List.of("searchKb", "searchHistory"), "generateAnswer")`
   - `addEdge("generateAnswer", StateGraph.END)`
3. **禁止** `AggregationStrategy.addAggregatedEdge` / `addAggregatedEdge`；`NodeAggregationStrategy.ALL_OF` 非必须（RESEARCH A3）
4. 并行节点用内存假数据保证成功（Pitfall 4），避免 curl 挂起
5. Controller：`GET /graph/parallel?question=` → `Result`
6. README（无中间件）+ api.http
  </action>
  <verify>
    <automated>mvn -f examples/39-graph-parallel-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18039
    - 源码含 addEdge( 与 List.of(，体现并行 fan-out/fan-in
    - 源码不含 addAggregatedEdge
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>39 compile 绿，并行边 JAR 真 API</done>
</task>

<task type="auto">
  <name>Task 3: 新建 40-graph-saga-demo（补偿节点）</name>
  <files>examples/40-graph-saga-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Saga 最小图片段）
    - docs/tutorial/14-Workflow.md（§14.7 补偿）
    - examples/38-workflow-demo/（骨架）
  </read_first>
  <action>
新建 `examples/40-graph-saga-demo/`（端口 18040，包 `com.flywhl.saa.graphsaga`，Claude's Discretion：最小补偿，无外部事务中间件）：
1. pom 同 38
2. 节点：`deductInventory` → `chargePayment` → 条件边：success→END / failure→`compensateInventory`→END
3. 内存 Map 模拟库存与扣款；`GET /graph/saga?orderId=&forceFail=true` 触发补偿路径（RESEARCH Saga 片段）
4. `addConditionalEdges("chargePayment", predicate, Map.of("success", END, "failure", "compensateInventory"))`（API 以 JAR 为准微调）
5. KeyStrategy.REPLACE；MemorySaver 可选
6. Controller 返回 `Result`，响应体标明是否走了补偿
7. README（无中间件）+ api.http（含 forceFail=true / false 两条）
禁止外部事务中间件、TODO、废弃 API
  </action>
  <verify>
    <automated>mvn -f examples/40-graph-saga-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18040
    - 源码含 compensateInventory 或等价补偿节点名，以及 forceFail 查询参数或等价触发
    - 源码含 addConditionalEdges 或条件边 API
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>40 compile 绿，Saga 补偿可 curl 演示</done>
</task>

</tasks>

<verification>
mvn -f examples/38-workflow-demo/pom.xml -q compile && mvn -f examples/39-graph-parallel-demo/pom.xml -q compile && mvn -f examples/40-graph-saga-demo/pom.xml -q compile
</verification>

<success_criteria>
- 38/39/40 均可独立 compile
- 线性 / 并行 / Saga 三种图形态可区分
- 无 addAggregatedEdge / KeyStrategy.replace() 伪 API
- 38 冒烟 IT 就位
</success_criteria>

<output>
Create `.planning/phases/03-48-demo/03-10-SUMMARY.md` when done
</output>
