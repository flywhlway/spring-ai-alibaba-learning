---
phase: 03-48-demo
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/44-stream-demo/
  - examples/45-observability-demo/
  - examples/46-logging-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 真机 curl / 冒烟 IT 需 AI_DASHSCOPE_API_KEY；编译不依赖 Key
  - 执行前须 mvn -pl common,starter -am -q -DskipTests install（D-10 starter）
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 35~48"
    - "D-03: 44~46 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-05: 端口 18044/18045/18046"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 API；AuditLoggingAdvisor 已是 CallAdvisor/StreamAdvisor"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-08b: 小 DTO 同包"
    - "D-09: common + Result + GlobalExceptionHandler"
    - "D-10: 44~46 强制引入 saa-learning-starter，复用 AuditLoggingAdvisor / CostTrackingObservationHandler / CostRecorder，禁止手写重实现"
    - "D-12: 44→17-Streaming；45/46→18-Observability；starter 装配参考 19-BestPractice"
    - "D-13: 章节映射锁定"
    - "D-15/D-16: 44/46 无中间件；45 README 写清 /actuator/prometheus，Grafana 仅文档"
    - "D-18: 44 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-20: Stream + Observability + Logging 能力域 plan（44~46）"
    - 44 SSE 事件 message/error/done；ChatClient.defaultAdvisors(audit)
    - 45 Micrometer + micrometer-registry-prometheus；复用 starter CostTrackingObservationHandler
    - 46 MDC traceId + AuditLoggingAdvisor；日志含同一 traceId
  artifacts:
    - examples/44-stream-demo/pom.xml
    - examples/44-stream-demo/README.md
    - examples/44-stream-demo/api.http
    - examples/45-observability-demo/pom.xml
    - examples/45-observability-demo/README.md
    - examples/45-observability-demo/api.http
    - examples/46-logging-demo/pom.xml
    - examples/46-logging-demo/README.md
    - examples/46-logging-demo/api.http
  key_links:
    - 44 → docs/tutorial/17-Streaming.md
    - 45/46 → docs/tutorial/18-Observability.md
    - starter 装配 → starter/src/main/java/.../SaaLearningAutoConfiguration.java
    - D-10 强制 starter
---

<objective>
新建 44-stream-demo、45-observability-demo、46-logging-demo，交付 Stream / Observability / Logging（强制 starter）。

Purpose: Batch 3 best-practice 前半；验证 starter 审计与成本装配，不重复实现。
Output: 三个可编译 Demo；44 含冒烟 IT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@.planning/phases/03-48-demo/03-PATTERNS.md
@.claude/skills/saa-conventions/SKILL.md
@docs/tutorial/17-Streaming.md
@docs/tutorial/18-Observability.md
@docs/tutorial/19-BestPractice.md
@starter/src/main/java/com/flywhl/saa/starter/autoconfigure/SaaLearningAutoConfiguration.java
@starter/src/main/java/com/flywhl/saa/starter/advisor/AuditLoggingAdvisor.java
@starter/src/main/java/com/flywhl/saa/starter/metrics/CostTrackingObservationHandler.java
@examples/04-chat-demo/
@examples/16-memory-demo/
@examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
</context>

<threat_model>
## Trust Boundaries
- 用户 message → ChatClient（SSE/同步）→ DashScope → 客户端
- Actuator/prometheus 端点（本机 Demo）

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-44-01 | MED | SSE error 事件泄露堆栈 | onErrorResume 写 Result.fail 业务码，不回传完整 stack |
| T-45-01 | MED | /actuator/prometheus 公网信息泄露 | Demo 仅本机；README 注明生产须鉴权/网络隔离 |
| T-46-01 | MED | 审计日志泄露 PII | 复用 AuditLoggingAdvisor 手机号脱敏；禁止自写未脱敏 Advisor |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 44-stream-demo（SSE + starter Advisor + 冒烟 IT）</name>
  <files>examples/44-stream-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Pattern 4 starter 装配；统一 SSE 片段）
    - docs/tutorial/17-Streaming.md（§17.5）
    - starter/src/main/java/com/flywhl/saa/starter/autoconfigure/SaaLearningAutoConfiguration.java
    - starter/src/main/java/com/flywhl/saa/starter/advisor/AuditLoggingAdvisor.java
    - examples/04-chat-demo/pom.xml
    - examples/16-memory-demo/src/main/java/com/flywhl/saa/memory/MemoryConfig.java
    - examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
    - .claude/skills/saa-conventions/SKILL.md
  </read_first>
  <action>
新建 `examples/44-stream-demo/`（端口 18044，包 `com.flywhl.saa.stream`，per D-05/D-10/D-12）：
1. 先 `mvn -pl common,starter -am -q -DskipTests install`
2. pom：web + dashscope + `saa-learning-common` + **`saa-learning-starter`** + test；零版本号
3. `server.port=18044`；`${AI_DASHSCOPE_API_KEY}`
4. `StreamConfig`：`@Bean ChatClient`，注入 `AuditLoggingAdvisor`，**显式** `builder.defaultAdvisors(audit).build()`（starter 不会自动挂 Advisor，RESEARCH Pitfall 6）
5. **禁止**手写 AuditLoggingAdvisor / CostRecorder / ModelRouter 实现类
6. `StreamController`：`GET /chat/stream-unified`，`produces = TEXT_EVENT_STREAM_VALUE`，返回 `Flux<ServerSentEvent<String>>`：
   - event `message`：流式文本片段
   - event `error`：ObjectMapper 序列化 `Result.fail(...)`
   - event `done`：结束标记
   （对齐 RESEARCH 统一 SSE 片段）
7. `@Import(GlobalExceptionHandler.class)`（starter 也可能 Import，重复无害）
8. README（无中间件）+ api.http
9. 冒烟 IT（D-18）：`StreamDemoApplicationIT`，`@EnabledIfEnvironmentVariable(named="AI_DASHSCOPE_API_KEY", matches=".+")`，断言 stream 至少一个非空 message 或 content 非空
  </action>
  <verify>
    <automated>mvn -f examples/44-stream-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - pom 含 saa-learning-starter 与 saa-learning-common
    - application.yml 含 server.port: 18044
    - 源码含 defaultAdvisors( 与 AuditLoggingAdvisor，含 ServerSentEvent 与 event message/done/error 之一组
    - 源码不含 class AuditLoggingAdvisor 自实现、不含 class CostRecorder 自实现
    - StreamDemoApplicationIT 含 @EnabledIfEnvironmentVariable(named = "AI_DASHSCOPE_API_KEY"
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>44 compile 绿，SSE + starter Advisor + 冒烟 IT</done>
</task>

<task type="auto">
  <name>Task 2: 新建 45-observability-demo（Prometheus + starter 成本）</name>
  <files>examples/45-observability-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（45 复用 CostTrackingObservationHandler）
    - docs/tutorial/18-Observability.md（yml + curl；忽略教程手写 Handler）
    - starter/src/main/java/com/flywhl/saa/starter/metrics/CostTrackingObservationHandler.java
    - examples/44-stream-demo/（Task 1 starter 装配模式）
  </read_first>
  <action>
新建 `examples/45-observability-demo/`（端口 18045，包 `com.flywhl.saa.observability`，per D-10/D-15）：
1. pom：web + dashscope + common + **starter** + `spring-boot-starter-actuator` + `micrometer-registry-prometheus`
2. application.yml：
   - `server.port=18045`
   - `management.endpoints.web.exposure.include: health,prometheus,metrics`（或等价）
   - `management.endpoint.prometheus.enabled: true`
   - DashScope key
3. ChatClient 挂 `AuditLoggingAdvisor`（与 44 同模式）
4. **禁止**复制教程 `@Component` 版 CostTracking Handler；依赖 starter 自动装配的 `CostTrackingObservationHandler` + `LoggingCostRecorder`（`saa.learning.cost-tracking.enabled` 默认 true）
5. Controller：至少一个 `GET /obs/chat?message=` 触发模型调用，便于 metrics 产生
6. README：写清 `curl localhost:18045/actuator/prometheus`；Grafana 看板仅文档说明，不强求 compose 内嵌（Claude's Discretion）；中间件默认无
7. api.http
  </action>
  <verify>
    <automated>mvn -f examples/45-observability-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - pom 含 saa-learning-starter、spring-boot-starter-actuator、micrometer-registry-prometheus
    - application.yml 含 server.port: 18045 与 prometheus 或 management.endpoints
    - 源码不含自写 CostTrackingObservationHandler 类定义
    - README 提及 /actuator/prometheus
    - compile 退出码 0
  </acceptance_criteria>
  <done>45 compile 绿，actuator+prometheus + starter 成本</done>
</task>

<task type="auto">
  <name>Task 3: 新建 46-logging-demo（MDC traceId + AuditLoggingAdvisor）</name>
  <files>examples/46-logging-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（46 logging）
    - docs/tutorial/18-Observability.md（结构化日志段落）
    - starter/src/main/java/com/flywhl/saa/starter/advisor/AuditLoggingAdvisor.java
    - examples/16-memory-demo/（Filter/会话模式参考）
  </read_first>
  <action>
新建 `examples/46-logging-demo/`（端口 18046，包 `com.flywhl.saa.logging`，per D-10）：
1. pom：web + dashscope + common + **starter**
2. `TraceIdFilter`（或 HandlerInterceptor）：为每个请求生成 UUID 写入 MDC key `traceId`，请求结束 clear
3. ChatClient：`defaultAdvisors(auditLoggingAdvisor)`
4. Controller：`GET /log/chat?message=` → `Result.ok(content)`；业务日志与 AUDIT logger 均应能关联同一 traceId（logging pattern 含 `%X{traceId}`，在 `application.yml` 或 `logback-spring.xml`）
5. **禁止**自写审计 Advisor；复用 starter `AuditLoggingAdvisor`
6. README（无中间件）说明如何在日志中看到 traceId；api.http
  </action>
  <verify>
    <automated>mvn -f examples/46-logging-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18046
    - pom 含 saa-learning-starter
    - 源码含 MDC.put 或 traceId 与 AuditLoggingAdvisor
    - 源码不含 class AuditLoggingAdvisor 自实现
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>46 compile 绿，traceId + starter 审计贯通</done>
</task>

</tasks>

<verification>
mvn -pl common,starter -am -q -DskipTests install && mvn -f examples/44-stream-demo/pom.xml -q compile && mvn -f examples/45-observability-demo/pom.xml -q compile && mvn -f examples/46-logging-demo/pom.xml -q compile
</verification>

<success_criteria>
- 44/45/46 均可独立 compile
- 均依赖 saa-learning-starter，无手写 ModelRouter/CostRecorder/AuditLoggingAdvisor
- 44 冒烟 IT 就位；45 暴露 /actuator/prometheus
</success_criteria>

<output>
Create `.planning/phases/03-48-demo/03-12-SUMMARY.md` when done
</output>
