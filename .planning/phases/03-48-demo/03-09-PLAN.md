---
phase: 03-48-demo
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/35-agent-demo/
  - examples/36-agent-skills-demo/
  - examples/37-agent-hitl-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 真机 curl / 冒烟 IT 需 AI_DASHSCOPE_API_KEY；编译不依赖 Key
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 35~48"
    - "D-03: 35~37 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-05: 端口 18035/18036/18037"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用 PromptChatMemoryAdvisor/CallAroundAdvisor/FunctionCallback/可变 Options setter"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-08b: 小 DTO/record 与使用方同包，勿拆 model 子包"
    - "D-09: common + Result + GlobalExceptionHandler"
    - "D-11: Chat/Agent 模型一律 DashScope"
    - "D-12: 规格以 docs/tutorial/13-Agent.md 可运行 Demo 为准"
    - "D-13: 35/36/37 → 教程第 13 章"
    - "D-14: 教程伪 API 按 03-RESEARCH JAR 映射纠偏"
    - "D-15/D-16: 35~37 README 写无中间件"
    - "D-18: 35 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-20: Agent 能力域 plan（35~37）"
    - 35 使用 ReactAgent + methodTools + ModelCallLimitHook + MemorySaver，禁止 .maxIterations / .tools(Component)
    - 36 使用 ClasspathSkillRegistry 或 FileSystemSkillRegistry + SkillsAgentHook/SkillsInterceptor，禁止 Skill.of
    - 37 使用 HumanInTheLoopHook.approvalOn，禁止 .interruptBefore 伪 API；REST start/approve 同 threadId
    - 三 Demo 均 compile，无硬编码密钥、无废弃 API、无 TODO
  artifacts:
    - examples/35-agent-demo/pom.xml
    - examples/35-agent-demo/README.md
    - examples/35-agent-demo/api.http
    - examples/36-agent-skills-demo/pom.xml
    - examples/36-agent-skills-demo/README.md
    - examples/36-agent-skills-demo/api.http
    - examples/37-agent-hitl-demo/pom.xml
    - examples/37-agent-hitl-demo/README.md
    - examples/37-agent-hitl-demo/api.http
  key_links:
    - 35/36/37 → docs/tutorial/13-Agent.md
    - API 真源 → 03-RESEARCH.md「教程 → 1.1.2.2 JAR API 映射」
    - D-09 common + Result
    - D-18 35 冒烟 IT
---

<objective>
新建 35-agent-demo、36-agent-skills-demo、37-agent-hitl-demo，交付 Agent 能力域（ReactAgent / Skills / HITL）。

Purpose: Batch 3 Wave 1 Agent 切片；以 JAR 真源纠偏教程伪 API。
Output: 三个可编译、可 curl 的独立 Demo；35 含冒烟 IT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@.planning/phases/03-48-demo/03-PATTERNS.md
@.claude/skills/saa-conventions/SKILL.md
@docs/tutorial/13-Agent.md
@examples/11-tool-demo/
@examples/04-chat-demo/
@examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
</context>

<threat_model>
## Trust Boundaries
- 用户查询参数 → ReactAgent → @Tool / Skills → DashScope API → REST JSON（Result）

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-35-01 | MED | Prompt 注入导致无限 tool 循环 | ModelCallLimitHook.runLimit(6)；响应经 Result，不执行模型原始指令 |
| T-37-01 | HIGH | HITL 高风险工具未审批即执行 | HumanInTheLoopHook.approvalOn("execute_payment")；threadId 用 UUID，禁止可预测自增 |
| T-37-02 | MED | threadId 枚举劫持会话 | UUID threadId；approve 必须携带同一 id；不暴露内部 OverAllState |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 35-agent-demo（ReactAgent + 冒烟 IT）</name>
  <files>examples/35-agent-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Pattern 1 ReactAgent；教程→JAR 映射表）
    - docs/tutorial/13-Agent.md（可运行 Demo / 车辆诊断场景）
    - examples/11-tool-demo/pom.xml
    - examples/11-tool-demo/src/main/java/com/flywhl/saa/tool/MemberTools.java
    - examples/11-tool-demo/src/main/java/com/flywhl/saa/tool/ToolDemoApplication.java
    - examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
    - .claude/skills/saa-conventions/SKILL.md
  </read_first>
  <action>
新建完整工程 `examples/35-agent-demo/`（per D-03/D-04/D-05/D-06/D-09）：
1. pom：parent `com.flywhl.saa:spring-ai-alibaba-learning:1.0.0-SNAPSHOT`，`relativePath=../../pom.xml`；依赖 `spring-boot-starter-web`、`spring-boot-starter-validation`、`spring-ai-alibaba-starter-dashscope`、`spring-ai-alibaba-agent-framework`、`saa-learning-common`、`spring-boot-starter-test`（test），零版本号；**不**引入 `saa-learning-starter`（D-10 仅 44~48）
2. `application.yml`：`server.port=18035`；`spring.ai.dashscope.api-key: ${AI_DASHSCOPE_API_KEY}`；模型 qwen-plus
3. 包 `com.flywhl.saa.agentdemo`；类头 `@author flywhl`
4. `DtcLookupTools`：`@Component` + `@Tool`（对齐 11-tool-demo MemberTools）；小 DTO 同包（D-08b）
5. `VehicleDiagnosisAgentConfig`：`@Bean ReactAgent`，按 RESEARCH Pattern 1：`ReactAgent.builder().name("vehicle-diagnosis-agent").model(dashScopeChatModel).systemPrompt(...).methodTools(dtcLookupTools).hooks(ModelCallLimitHook.builder().runLimit(6).build()).saver(new MemorySaver()).build()`
6. **禁止**教程伪 API：`.tools(Component)`、`.maxIterations(6)`、`PromptChatMemoryAdvisor`、`CallAroundAdvisor`、`FunctionCallback`
7. `AgentController`：如 `GET /agent/diagnose?query=`，`agent.call(query)` → `AssistantMessage.getText()` → `Result.ok(...)`
8. `AgentDemoApplication`：`@SpringBootApplication` + `@Import(GlobalExceptionHandler.class)`
9. README（前置条件：无中间件，D-16）+ `api.http` + curl 与预期输出
10. 冒烟 IT（D-18）：`AgentDemoApplicationIT`，`@SpringBootTest` + `@EnabledIfEnvironmentVariable(named="AI_DASHSCOPE_API_KEY", matches=".+")`，注入 `ReactAgent`，断言 `call(...).getText()` 非空白
执行前：`mvn -pl common -am -q -DskipTests install`（若 common 未 install）
  </action>
  <verify>
    <automated>mvn -f examples/35-agent-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - examples/35-agent-demo/pom.xml parent relativePath 为 ../../pom.xml，含 spring-ai-alibaba-agent-framework 与 saa-learning-common，无 saa-learning-starter
    - application.yml 含 server.port: 18035 与 ${AI_DASHSCOPE_API_KEY}
    - 源码含 methodTools( 与 ModelCallLimitHook，不含 maxIterations、Skill.of、SupervisorAgent
    - AgentDemoApplicationIT 含 @EnabledIfEnvironmentVariable(named = "AI_DASHSCOPE_API_KEY"
    - README.md 与 api.http 存在；README 声明无中间件
    - compile 退出码 0
  </acceptance_criteria>
  <done>35 compile 绿，ReactAgent 规格对齐 RESEARCH Pattern 1，冒烟 IT 就位</done>
</task>

<task type="auto">
  <name>Task 2: 新建 36-agent-skills-demo（Skills Hook + Registry）</name>
  <files>examples/36-agent-skills-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Skills 映射：ClasspathSkillRegistry / SkillsAgentHook）
    - docs/tutorial/13-Agent.md（Skills / 渐进式披露段落）
    - examples/35-agent-demo/（Task 1 产物骨架）
    - examples/README.md（36 演示要点）
  </read_first>
  <action>
新建 `examples/36-agent-skills-demo/`（端口 18036，包 `com.flywhl.saa.agentskills`，per D-05/D-13/D-14）：
1. pom 依赖同 35（agent-framework + dashscope + common），无 starter
2. Skills 按 RESEARCH：**禁止** `Skill.of(...)` / `.skills(List.of(...))`
3. 使用 `ClasspathSkillRegistry` 或 `FileSystemSkillRegistry`（graph-core，由 agent-framework 传递）+ `SkillsAgentHook` 和/或 `SkillsInterceptor` 挂到 `ReactAgent.builder().hooks(...)`
4. 在 `src/main/resources/skills/`（或 ClasspathSkillRegistry 要求的路径）放置至少一个 `SKILL.md` 资源；execute 时若 classpath 布局不明，fallback 用 `FileSystemSkillRegistry` 指向该目录（RESEARCH Open Question 1）
5. Controller：`GET /agent/skills?query=`（或教程等价路径），返回 `Result<String>`
6. `@Import(GlobalExceptionHandler.class)`；README（无中间件）+ api.http
7. 可选：`ModelCallLimitHook` 限制循环
禁止废弃 API / 硬编码密钥 / TODO / 拆 model 子包
  </action>
  <verify>
    <automated>mvn -f examples/36-agent-skills-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18036
    - 源码含 SkillRegistry 或 ClasspathSkillRegistry 或 FileSystemSkillRegistry，以及 SkillsAgentHook 或 SkillsInterceptor
    - 源码不含 Skill.of
    - resources 下存在 SKILL.md
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>36 compile 绿，Skills 走 Hook+Registry 真 API</done>
</task>

<task type="auto">
  <name>Task 3: 新建 37-agent-hitl-demo（HITL 暂停/恢复）</name>
  <files>examples/37-agent-hitl-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（HITL 最小 REST；HumanInTheLoopHook.approvalOn）
    - docs/tutorial/13-Agent.md（HITL 段落）
    - examples/16-memory-demo/src/main/java/com/flywhl/saa/memory/MemoryController.java（会话 id 模式）
    - examples/35-agent-demo/（骨架）
  </read_first>
  <action>
新建 `examples/37-agent-hitl-demo/`（端口 18037，包 `com.flywhl.saa.agenthitl`，per D-05 与 Claude's Discretion HITL）：
1. pom 同 35
2. `HighRiskTools`（或等价）：含 `@Tool` 方法名 `execute_payment`（高风险）
3. `HumanInTheLoopHook.builder().approvalOn("execute_payment", "支付操作需人工确认").build()` + `ModelCallLimitHook` + `MemorySaver` 挂到 ReactAgent
4. **禁止** `.interruptBefore("execute_payment")` 伪 API
5. REST（最小暂停→确认→恢复）：
   - `POST /hitl/start`：生成 UUID `threadId`，`agent.invoke(input, RunnableConfig.builder().threadId(id).build())`，返回 threadId + 中断/待审批状态（InterruptionMetadata 语义以 JAR 为准）
   - `POST /hitl/approve?threadId=`：同一 threadId 恢复执行
6. 响应 `Result<T>`；DTO 同包
7. README 写明 start→approve 流程与无中间件；api.http 含两条请求
禁止可预测自增 threadId（T-37-02）
  </action>
  <verify>
    <automated>mvn -f examples/37-agent-hitl-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18037
    - 源码含 HumanInTheLoopHook 与 approvalOn，不含 interruptBefore(
    - Controller 含 /hitl/start 与 /hitl/approve
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>37 compile 绿，HITL REST 可演示暂停/恢复</done>
</task>

</tasks>

<verification>
mvn -f examples/35-agent-demo/pom.xml -q compile && mvn -f examples/36-agent-skills-demo/pom.xml -q compile && mvn -f examples/37-agent-hitl-demo/pom.xml -q compile
</verification>

<success_criteria>
- 35/36/37 均可独立 compile
- 端口 18035~18037，common+Result，密钥仅环境变量
- API 使用 RESEARCH JAR 映射，无教程伪 API
- 35 冒烟 IT 就位（有 Key 时可跑）
</success_criteria>

<output>
Create `.planning/phases/03-48-demo/03-09-SUMMARY.md` when done
</output>
