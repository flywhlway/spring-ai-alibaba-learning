---
phase: 03-48-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/01-quickstart-demo/
  - examples/02-autoconfig-demo/
  - examples/03-multi-model-demo/
  - examples/04-chat-demo/
  - examples/06-prompt-demo/
  - examples/07-prompt-builder-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup: []
must_haves:
  truths:
    - common 已 install，可供 Demo 依赖
    - 既有 Demo 01/02/03/04/06/07 均能 compile
    - 无硬编码密钥、无废弃 API、无 TODO
  artifacts:
    - examples/0{1,2,3,4,6,7}-*/pom.xml
    - examples/0{1,2,3,4,6,7}-*/README.md
    - examples/0{1,2,3,4,6,7}-*/api.http
  key_links:
    - parent POM relativePath → 仓库根 pom.xml
    - 04/06/07 → saa-learning-common
---

<objective>
审计并修齐限额中断后已落盘的 Demo 01/02/03/04/06/07，确保符合 HANDOFF §1.4 / §3，且 `mvn compile` 通过。

Purpose: 安全续接，不为已完成 Demo 重写，只修违规与编译失败。
Output: 六个 Demo 可编译；common 已安装到本地仓库。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.claude/skills/saa-conventions/SKILL.md
@HANDOFF-TO-CLAUDE-CODE.md
@examples/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 common（及 starter 若被依赖）</name>
  <files>common/, starter/</files>
  <read_first>pom.xml, CLAUDE.md</read_first>
  <action>执行 `mvn -pl common,starter -am clean install -DskipTests`（若 starter 失败则至少 `mvn -pl common -am clean install`）。确保 `saa-learning-common` 在本地仓库可用。</action>
  <verify>mvn -pl common -am -q -DskipTests install 退出码 0</verify>
  <acceptance_criteria>
    - common 模块 install 成功
  </acceptance_criteria>
  <done>common 在本地 m2 可被 examples 依赖解析</done>
</task>

<task type="auto">
  <name>Task 2: 审计既有 Demo 01/02/03/04/06/07</name>
  <files>examples/01-quickstart-demo/, examples/02-autoconfig-demo/, examples/03-multi-model-demo/, examples/04-chat-demo/, examples/06-prompt-demo/, examples/07-prompt-builder-demo/</files>
  <read_first>.planning/phases/03-48-demo/03-CONTEXT.md, HANDOFF-TO-CLAUDE-CODE.md</read_first>
  <action>
对每个 Demo 检查并仅在违规时修复：
1. 包根 `com.flywhl.saa.*`，`@author flywhl`
2. `server.port=180NN`（NN=目录编号）
3. 密钥仅 `${AI_DASHSCOPE_API_KEY}` / `${DEEPSEEK_API_KEY}`
4. 无 `PromptChatMemoryAdvisor` / `CallAroundAdvisor` / `FunctionCallback` / TODO
5. 存在 README.md + api.http
6. parent 指向仓库父 POM，无硬编码 SAA 版本号
7. `mvn -f examples/NN-xxx/pom.xml -q compile` 通过
不重写业务逻辑；01~03 保持教程最小形态（可不包 Result）。
  </action>
  <verify>对六个目录分别 compile 退出码 0</verify>
  <acceptance_criteria>
    - 六个 Demo compile 全绿
    - rg 无硬编码 sk-/API key 模式
  </acceptance_criteria>
  <done>既有六个 Demo 可编译且约定合规</done>
</task>

</tasks>

<verification>
- [ ] `mvn -pl common -am -DskipTests install` 成功
- [ ] 六个 Demo 各自 `mvn -f ... -q compile` 成功
</verification>

<success_criteria>
- 既有 Demo 安全续接完成，无破坏性重写
- 为 Plan 02（05/08）扫清依赖与模式基线
</success_criteria>
