---
phase: 03-48-demo
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/27-rag-demo/
  - examples/28-advanced-rag-demo/
  - examples/29-hybrid-rag-demo/
  - examples/30-rag-eval-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 27~30 真机需 bash scripts/infra.sh up vector（默认 Milvus）与 AI_DASHSCOPE_API_KEY
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 20~34"
    - "D-03: 20~34 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 API"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-09: common + Result"
    - "D-11: Embedding 一律 DashScope"
    - "D-12: 规格以 docs/tutorial/09-RAG.md 为准"
    - "D-13: 27/28/29/30 → 教程第 09 章"
    - "D-14: 27/28/30 按同章 API 补齐最小可运行形态"
    - "D-15: RAG 默认 vector profile（Milvus）"
    - "D-18: 27 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-19: examples 独立应用"
    - 27 使用 QuestionAnswerAdvisor（Naive RAG）
    - 28 使用 RetrievalAugmentationAdvisor（查询改写/重排）
    - 29 返回 answer + citations（应用层拼接 metadata.source）
    - 30 提供最小评测端点（忠实度/相关性）
    - 默认向量后端 Milvus，dimensions=1024
    - 四 Demo 均 compile
  artifacts:
    - examples/27-rag-demo/pom.xml
    - examples/28-advanced-rag-demo/pom.xml
    - examples/29-hybrid-rag-demo/pom.xml
    - examples/30-rag-eval-demo/pom.xml
  key_links:
    - 27~30 → docs/tutorial/09-RAG.md
    - D-15 RAG 默认 vector profile
    - T-27-01 空上下文兜底
---

<objective>
新建 RAG 四件套（27~30）：Naive → Advanced → Hybrid+Citation → Eval。

Purpose: 交付次批 RAG 能力域，对齐企业项目一技术主干。
Output: 四个可编译 Demo；29 对齐教程完整示例。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@docs/tutorial/09-RAG.md
@docs/tutorial/11-VectorStore.md
@examples/16-memory-demo/
</context>

<threat_model>
## Trust Boundaries
- 用户问题 → 检索文档片段 → 拼入 Prompt → 模型回答

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-27-01 | HIGH | 文档投毒 / 检索片段 prompt injection | system 提示仅依据检索内容；空上下文兜底不编造；Citation 来自 metadata 非模型自述 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 27-rag-demo（Naive RAG）</name>
  <files>examples/27-rag-demo/</files>
  <read_first>
    - docs/tutorial/09-RAG.md（§9.3 QuestionAnswerAdvisor + ETL 概念）
    - docs/tutorial/09-RAG.md（29 可运行 Demo 的 yml/milvus 配置可复用）
  </read_first>
  <action>
新建 `examples/27-rag-demo/`（端口 18027，包 `com.flywhl.saa.rag`）：
1. 依赖：dashscope + `spring-ai-starter-vector-store-milvus` + `spring-ai-advisors-vector-store`（若 advisors 未由其他 starter 传递）+ common
2. yml：Milvus + embedding text-embedding-v4 dimensions=1024（对齐 29 教程配置）
3. `QuestionAnswerAdvisor.builder(vectorStore).build()` 挂到 ChatClient
4. `POST /ingest`（或 `/documents`）写入样例 Document（含 metadata.source）；`GET /ask?question=` 返回答案 Result
5. system 提示约束仅依据知识库；README：`bash scripts/infra.sh up vector`
6. 冒烟 IT：有 Key 时可测（无 Milvus 时允许 Disabled）
  </action>
  <verify>mvn -f examples/27-rag-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - 源码含 QuestionAnswerAdvisor
    - server.port=18027
    - README 含 infra.sh up vector
    - compile 退出码 0
  </acceptance_criteria>
  <done>27 compile 绿</done>
</task>

<task type="auto">
  <name>Task 2: 新建 28-advanced-rag-demo</name>
  <files>examples/28-advanced-rag-demo/</files>
  <read_first>
    - docs/tutorial/09-RAG.md（§9.4 RetrievalAugmentationAdvisor 全家桶）
  </read_first>
  <action>
新建 `examples/28-advanced-rag-demo/`（端口 18028，包 `com.flywhl.saa.advancedrag`）：
1. 依赖同 27
2. 使用 `RetrievalAugmentationAdvisor`，至少配置：VectorStoreDocumentRetriever + 查询改写（RewriteQueryTransformer 或教程等价 API）和/或 DocumentRanker/重排序组件（以 Spring AI 1.1.2 实际类名为准，读依赖源码/教程）
3. ingest + ask 端点；README 说明 advanced 相对 naive 的差异
禁止 CallAroundAdvisor / 废弃 API
  </action>
  <verify>mvn -f examples/28-advanced-rag-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - 源码含 RetrievalAugmentationAdvisor
    - server.port=18028
    - compile 退出码 0
  </acceptance_criteria>
  <done>28 compile 绿</done>
</task>

<task type="auto">
  <name>Task 3: 新建 29-hybrid-rag-demo</name>
  <files>examples/29-hybrid-rag-demo/</files>
  <read_first>
    - docs/tutorial/09-RAG.md（可运行 Demo：Hybrid RAG + Citation，完整代码）
  </read_first>
  <action>
新建 `examples/29-hybrid-rag-demo/`（端口 18029，包 `com.flywhl.saa.hybridrag`）：
1. 按教程 RagController：VectorStoreDocumentRetriever + RetrievalAugmentationAdvisor
2. `/ask` 返回 answer、citations（从 metadata.source 应用层拼接）、evidenceCount；用 Result 包装 Map 或专用 VO
3. yml 对齐教程 milvus/embedding 配置
4. README + api.http，curl 示例与教程一致
  </action>
  <verify>mvn -f examples/29-hybrid-rag-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - 源码含 citations 与 RetrievalAugmentationAdvisor
    - server.port=18029
    - application.yml 含 collection-name 或等价 milvus 配置
    - compile 退出码 0
  </acceptance_criteria>
  <done>29 compile 绿，Citation 应用层拼接</done>
</task>

<task type="auto">
  <name>Task 4: 新建 30-rag-eval-demo</name>
  <files>examples/30-rag-eval-demo/</files>
  <read_first>
    - docs/tutorial/09-RAG.md（评测/缓存相关叙述）
    - examples/README.md（30 演示要点）
  </read_first>
  <action>
新建 `examples/30-rag-eval-demo/`（端口 18030，包 `com.flywhl.saa.rageval`）：
1. 依赖同 27/29（Milvus + RAG advisor）
2. 最小评测：内置或 POST 固定问答集；对每条计算简单 faithfulness/relevance 分数（可用规则：答案是否包含证据关键词 / 二次 LLM-as-judge 二选一，优先可编译的最小实现）
3. 可选：简单响应缓存（内存 Map 或 Spring Cache）演示降本
4. REST：`POST /eval/run` 返回评分列表；README 说明指标含义与局限
  </action>
  <verify>mvn -f examples/30-rag-eval-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - server.port=18030
    - 存在 /eval 相关映射
    - README 提及忠实度或相关性
    - compile 退出码 0
  </acceptance_criteria>
  <done>30 compile 绿</done>
</task>

</tasks>

<verification>
for d in 27-rag-demo 28-advanced-rag-demo 29-hybrid-rag-demo 30-rag-eval-demo; do mvn -f "examples/$d/pom.xml" -q compile || exit 1; done
</verification>

<success_criteria>
- 27~30 compile 全绿
- Naive vs Modular RAG API 区分清晰；29 含 citations
</success_criteria>
