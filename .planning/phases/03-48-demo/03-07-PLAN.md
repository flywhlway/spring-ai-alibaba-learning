---
phase: 03-48-demo
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/31-mcp-server-demo/
  - examples/32-mcp-client-demo/
  - examples/33-mcp-auth-demo/
  - examples/34-mcp-nacos-demo/order-mcp-server/
  - examples/34-mcp-nacos-demo/office-assistant-client/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 32 真机 curl 需先启动 31（http://localhost:18031/mcp）
  - 34 真机需 bash scripts/infra.sh up cloud（Nacos）
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 20~34"
    - "D-03: 20~34 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-05: 端口 180NN；34 Client = Server+100 = 18134"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 API"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-09: common + Result（Client 侧）"
    - "D-10: 本批不引入 saa-learning-starter"
    - "D-12: 规格以 docs/tutorial/12-MCP.md 为准"
    - "D-13: 31/32/33/34 → 教程第 12 章"
    - "D-14: 31/32/33 按同章 API 补齐"
    - "D-15: 31~33 无中间件；34=cloud（Nacos）"
    - "D-16: 31 README 写无中间件"
    - "D-18: 31 冒烟 IT（context 加载 / McpTool bean）"
    - "D-19: examples 独立应用"
    - 31 暴露 @McpTool Streamable HTTP，端口 18031
    - 32 通过 MCP Client 消费 31，端口 18032
    - 33 演示 Bearer TransportContextExtractor 鉴权，端口 18033
    - 34 Server 18034 + Client 18134，Nacos 服务发现
    - MCP 注解包为 Spring AI 1.1.2 路径（非 2.0 org.springframework.ai.mcp.annotation）
    - 全部 compile（含 34 两子模块）
  artifacts:
    - examples/31-mcp-server-demo/pom.xml
    - examples/32-mcp-client-demo/pom.xml
    - examples/33-mcp-auth-demo/pom.xml
    - examples/34-mcp-nacos-demo/order-mcp-server/pom.xml
    - examples/34-mcp-nacos-demo/office-assistant-client/pom.xml
  key_links:
    - 31~34 → docs/tutorial/12-MCP.md
    - D-05 Client = Server+100
    - T-31-01 / T-32-01 / T-34-01
---

<objective>
新建 MCP 四件套（31~34）：Server、Client、Auth、Nacos Registry。

Purpose: 交付次批 MCP 能力域，对齐企业项目二工具互通主干。
Output: 四个工程（34 为双模块），均可 compile。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@docs/tutorial/12-MCP.md
@examples/11-tool-demo/
</context>

<threat_model>
## Trust Boundaries
- MCP Client / 外部调用方 → HTTP 传输层 → MCP Server 工具

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-31-01 | HIGH | 未鉴权工具被任意调用 | 33 演示 Bearer；31 README 标注生产必须鉴权 |
| T-32-01 | MED | Client 聚合过多工具扩大攻击面 | 32 仅配置本机 31 一个连接 |
| T-34-01 | MED | Nacos 默认凭据 | 仅 dev；README 声明生产须更换 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 31-mcp-server-demo</name>
  <files>examples/31-mcp-server-demo/</files>
  <read_first>
    - docs/tutorial/12-MCP.md（§12.3 Server 注解 + 依赖 spring-ai-starter-mcp-server-webmvc）
    - examples/11-tool-demo/src/main/java/com/flywhl/saa/tool/MemberTools.java
  </read_first>
  <action>
新建 `examples/31-mcp-server-demo/`（端口 18031，包 `com.flywhl.saa.mcpserver`）：
1. 依赖：`spring-ai-starter-mcp-server-webmvc` + web（若需要健康检查 REST）+ common（可选）
2. `@Service` + `@McpTool` / `@McpToolParam` 暴露至少 1 个业务工具（如订单状态查询，返回固定演示数据）
3. 使用 Spring AI 1.1.2 注解包路径（教程：`org.springframework.ai.tool.annotation.McpTool`）；禁止 2.0 包名
4. README：无中间件；说明 MCP endpoint（通常 `/mcp`）；标注生产须鉴权
5. 冒烟 IT：Spring context 加载且含 McpTool bean（可不调模型）
  </action>
  <verify>mvn -f examples/31-mcp-server-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - pom 含 spring-ai-starter-mcp-server-webmvc
    - 源码含 @McpTool
    - server.port=18031
    - 无 org.springframework.ai.mcp.annotation 导入
    - compile 退出码 0
  </acceptance_criteria>
  <done>31 compile 绿</done>
</task>

<task type="auto">
  <name>Task 2: 新建 32-mcp-client-demo</name>
  <files>examples/32-mcp-client-demo/</files>
  <read_first>
    - docs/tutorial/12-MCP.md（§12.5 Client 配置 + SyncMcpToolCallbackProvider）
  </read_first>
  <action>
新建 `examples/32-mcp-client-demo/`（端口 18032，包 `com.flywhl.saa.mcpclient`）：
1. 依赖：mcp client starter（`spring-ai-starter-mcp-client-webmvc` 或 BOM 中实际坐标）+ dashscope + common
2. yml：`spring.ai.mcp.client.streamable-http.connections` 指向 `http://localhost:18031/mcp`（仅此一个连接）
3. ChatClient `.defaultTools(new SyncMcpToolCallbackProvider(...))`；`GET/POST /ask?question=`
4. README：启动顺序先 31 后 32；编译不依赖 31 运行
  </action>
  <verify>mvn -f examples/32-mcp-client-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - server.port=18032
    - application.yml 含 18031 与 /mcp
    - 源码含 SyncMcpToolCallbackProvider 或等价 MCP ToolCallback 集成
    - compile 退出码 0
  </acceptance_criteria>
  <done>32 compile 绿</done>
</task>

<task type="auto">
  <name>Task 3: 新建 33-mcp-auth-demo</name>
  <files>examples/33-mcp-auth-demo/</files>
  <read_first>
    - docs/tutorial/12-MCP.md（§12.4 TransportContextExtractor + McpSyncRequestContext）
  </read_first>
  <action>
新建 `examples/33-mcp-auth-demo/`（端口 18033，包 `com.flywhl.saa.mcpauth`）：
1. MCP Server + `TransportContextExtractor` 从 Authorization header 提取 bearer token
2. `@McpTool` 内校验 token（固定演示密钥如 `demo-secret`，仅来自传输层，不接受模型参数中的 token）
3. 无/错 token 时返回明确错误信息
4. README：curl 带 `Authorization: Bearer demo-secret` 的验证步骤
  </action>
  <verify>mvn -f examples/33-mcp-auth-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - server.port=18033
    - 源码含 TransportContextExtractor 或 contextExtractor 与 authorization
    - README 含 Bearer
    - compile 退出码 0
  </acceptance_criteria>
  <done>33 compile 绿，鉴权演示完整</done>
</task>

<task type="auto">
  <name>Task 4: 新建 34-mcp-nacos-demo（双模块）</name>
  <files>
    examples/34-mcp-nacos-demo/order-mcp-server/
    examples/34-mcp-nacos-demo/office-assistant-client/
  </files>
  <read_first>
    - docs/tutorial/12-MCP.md（可运行 Demo：MCP Server + Nacos 注册，完整 yml/代码）
  </read_first>
  <action>
按教程创建双模块工程：
1. `examples/34-mcp-nacos-demo/order-mcp-server/`：port 18034，包 `com.flywhl.saa.mcpnacos.server`，OrderTools `@McpTool getOrderStatus`，Nacos MCP 注册配置（`spring.ai.alibaba.mcp.nacos` / 教程键名），服务名 `order-service-mcp`
2. `examples/34-mcp-nacos-demo/office-assistant-client/`：port **18134**，包 `com.flywhl.saa.mcpnacos.client`，按服务名发现（非硬编码 IP），ChatClient `/ask`
3. 两模块各自独立 pom（parent 仓库根），各自 README 或根 README 说明启动顺序：infra cloud → server → client
4. 顶层 `examples/34-mcp-nacos-demo/README.md` 汇总
禁止硬编码生产密钥；Nacos 用 dev 地址 127.0.0.1:8848
  </action>
  <verify>
mvn -f examples/34-mcp-nacos-demo/order-mcp-server/pom.xml -q compile && mvn -f examples/34-mcp-nacos-demo/office-assistant-client/pom.xml -q compile
  </verify>
  <acceptance_criteria>
    - order-mcp-server application.yml 含 server.port: 18034
    - office-assistant-client application.yml 含 server.port: 18134
    - 源码含 getOrderStatus 与 order-service-mcp 配置引用
    - 两模块 compile 退出码 0
  </acceptance_criteria>
  <done>34 双模块 compile 绿，端口 18034/18134</done>
</task>

</tasks>

<verification>
mvn -f examples/31-mcp-server-demo/pom.xml -q compile && \
mvn -f examples/32-mcp-client-demo/pom.xml -q compile && \
mvn -f examples/33-mcp-auth-demo/pom.xml -q compile && \
mvn -f examples/34-mcp-nacos-demo/order-mcp-server/pom.xml -q compile && \
mvn -f examples/34-mcp-nacos-demo/office-assistant-client/pom.xml -q compile
</verification>

<success_criteria>
- 31~34 全部 compile
- 端口与 Server/Client 偏移正确；鉴权与 Nacos 演示点齐全
</success_criteria>
