---
phase: 03-48-demo
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/47-routing-demo/
  - examples/48-fallback-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 真机 curl / 冒烟 IT 需 AI_DASHSCOPE_API_KEY；47/48 完整双模型路径需 DEEPSEEK_API_KEY
  - 执行前须 mvn -pl common,starter -am -q -DskipTests install
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 35~48"
    - "D-03: 47~48 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-05: 端口 18047/18048"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 API"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-08b: 小 DTO 同包"
    - "D-09: common + Result + GlobalExceptionHandler"
    - "D-10: 47/48 强制 saa-learning-starter，复用 ModelRouter / FallbackModelRouter / CostRecorder / AuditLoggingAdvisor，禁止重实现"
    - "D-11: DashScope 主模型；DeepSeek 备（DEEPSEEK_API_KEY），对齐 03-multi-model-demo"
    - "D-12: 规格以 docs/tutorial/20-企业实践.md 为准；starter 参考 19-BestPractice"
    - "D-13: 47/48 → 教程第 20 章"
    - "D-15/D-16: README 写无中间件（多模型 Key）"
    - "D-18: 47 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-20: Routing + Fallback 能力域 plan（47~48）"
    - 47 ModelRouter Bean 条件：容器存在 dashScopeChatModel + deepSeekChatModel
    - 48 聚焦 reportFailure 降级：GET /fallback/status（isFallbackActive）+ 模拟主模型失败路径
  artifacts:
    - examples/47-routing-demo/pom.xml
    - examples/47-routing-demo/README.md
    - examples/47-routing-demo/api.http
    - examples/48-fallback-demo/pom.xml
    - examples/48-fallback-demo/README.md
    - examples/48-fallback-demo/api.http
  key_links:
    - 47/48 → docs/tutorial/20-企业实践.md
    - 双模型 yml → examples/03-multi-model-demo/
    - ModelRouter → starter FallbackModelRouter / SaaLearningAutoConfiguration
    - D-10 强制 starter
---

<objective>
新建 47-routing-demo、48-fallback-demo，交付多模型路由与降级（强制 starter ModelRouter）。

Purpose: Batch 3 best-practice 后半；验证 starter 主备路由与 reportFailure 熔断语义。
Output: 两个可编译 Demo；47 含冒烟 IT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@.planning/phases/03-48-demo/03-PATTERNS.md
@.claude/skills/saa-conventions/SKILL.md
@docs/tutorial/20-企业实践.md
@docs/tutorial/19-BestPractice.md
@starter/src/main/java/com/flywhl/saa/starter/autoconfigure/SaaLearningAutoConfiguration.java
@starter/src/main/java/com/flywhl/saa/starter/routing/ModelRouter.java
@starter/src/main/java/com/flywhl/saa/starter/routing/FallbackModelRouter.java
@examples/03-multi-model-demo/
@examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
</context>

<threat_model>
## Trust Boundaries
- 用户 query → ModelRouter.route() → primary/fallback ChatModel → DashScope/DeepSeek API

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-47-01 | MED | 密钥硬编码 | 仅 ${AI_DASHSCOPE_API_KEY} / ${DEEPSEEK_API_KEY} |
| T-48-01 | LOW | 降级状态接口被滥用探测 | Demo 本机；status 仅返回 isFallbackActive 布尔，无内部密钥/堆栈 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 47-routing-demo（ModelRouter + 冒烟 IT）</name>
  <files>examples/47-routing-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（Pattern 4；Pitfall 3 ModelRouter Bean）
    - docs/tutorial/20-企业实践.md（路由 Controller / §20.2）
    - starter/src/main/java/com/flywhl/saa/starter/autoconfigure/SaaLearningAutoConfiguration.java
    - starter/src/main/java/com/flywhl/saa/starter/routing/ModelRouter.java
    - examples/03-multi-model-demo/pom.xml
    - examples/03-multi-model-demo/src/main/resources/application.yml
    - examples/03-multi-model-demo/src/main/java/com/flywhl/saa/multimodel/MultiModelController.java
    - examples/20-structured-output-demo/src/test/java/com/flywhl/saa/structured/StructuredOutputDemoApplicationIT.java
    - .claude/skills/saa-conventions/SKILL.md
  </read_first>
  <action>
新建 `examples/47-routing-demo/`（端口 18047，包 `com.flywhl.saa.routing`，per D-10/D-11/D-12）：
1. `mvn -pl common,starter -am -q -DskipTests install`
2. pom：web + `spring-ai-alibaba-starter-dashscope` + `spring-ai-starter-model-deepseek` + `saa-learning-common` + **`saa-learning-starter`** + test（对齐 03-multi-model-demo + starter）
3. application.yml：
   - `server.port=18047`
   - `spring.ai.dashscope.api-key: ${AI_DASHSCOPE_API_KEY}`
   - `spring.ai.deepseek.api-key: ${DEEPSEEK_API_KEY}`
   - 模型选项对齐 03（qwen-plus / deepseek-chat）
   - 可选 `saa.learning.primary-model: dashScopeChatModel` / `fallback-model: deepSeekChatModel`（默认已是）
4. **关键**：自动装配 Bean 名必须为 `dashScopeChatModel` 与 `deepSeekChatModel`（SaaLearningAutoConfiguration `@ConditionalOnBean(name=...)`）。若自动装配名不符，显式 `@Bean("dashScopeChatModel")` / `@Bean("deepSeekChatModel")` 包装（RESEARCH A1）
5. Controller：注入 `ModelRouter`，`ChatModel model = modelRouter.route()`，`ChatClient.builder(model).defaultAdvisors(audit).build().prompt().user(q).call().content()` → `Result.ok`
6. 可选：教程 §20.2 `CostAwareRoutingPolicy` 叠在 ModelRouter 外（仅装配层，不复制 FallbackModelRouter 内部状态机）
7. **禁止**手写 `class FallbackModelRouter` / `class ModelRouter` 实现
8. README（无中间件；声明双 Key）+ api.http
9. 冒烟 IT（D-18）：`RoutingDemoApplicationIT`，`@EnabledIfEnvironmentVariable(named="AI_DASHSCOPE_API_KEY", matches=".+")`，断言 `ModelRouter` Bean 非空且 `route()` 非空；有 Key 时一次 call 返回非空白（若无 DEEPSEEK_API_KEY，contextLoads 至少验证 DashScope 路径或条件跳过双模型——优先保证有 DashScope 时 IT 绿）
  </action>
  <verify>
    <automated>mvn -f examples/47-routing-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - pom 含 saa-learning-starter、spring-ai-alibaba-starter-dashscope、spring-ai-starter-model-deepseek
    - application.yml 含 server.port: 18047、AI_DASHSCOPE_API_KEY、DEEPSEEK_API_KEY
    - 源码注入 ModelRouter 并调用 route()
    - 源码不含 class FallbackModelRouter 或 class 实现 ModelRouter 的自写类
    - RoutingDemoApplicationIT 含 @EnabledIfEnvironmentVariable(named = "AI_DASHSCOPE_API_KEY"
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>47 compile 绿，ModelRouter 装配 + 冒烟 IT</done>
</task>

<task type="auto">
  <name>Task 2: 新建 48-fallback-demo（reportFailure + isFallbackActive）</name>
  <files>examples/48-fallback-demo/</files>
  <read_first>
    - .planning/phases/03-48-demo/03-RESEARCH.md（48 fallback；Pattern 4 catch+reportFailure）
    - docs/tutorial/20-企业实践.md（降级段落）
    - starter/src/main/java/com/flywhl/saa/starter/routing/FallbackModelRouter.java
    - starter/src/test/java/com/flywhl/saa/starter/routing/FallbackModelRouterTest.java
    - examples/47-routing-demo/（Task 1 产物）
  </read_first>
  <action>
新建 `examples/48-fallback-demo/`（端口 18048，包 `com.flywhl.saa.fallback`，per D-10）：
1. pom / yml 同 47（双模型 + starter + common），端口 18048
2. Controller 聚焦降级演示：
   - `GET /fallback/chat?message=`：`model = modelRouter.route()`；try call；catch 后 `modelRouter.reportFailure(model, e)`，再 `modelRouter.route()` 用备用模型重试（RESEARCH Pattern 4）
   - `GET /fallback/status`：若注入类型为 `FallbackModelRouter`（或强转），返回 `isFallbackActive()`；否则返回路由状态摘要。响应 `Result<Map>` 或 record，仅布尔/状态字段
3. 提供可演示主模型失败的路径：如 `forceFail=true` 时在调用前对 primary 连续 `reportFailure` 达到阈值（默认 3，见 FallbackModelRouter 构造），使 `isFallbackActive()==true`，便于无真实故障时 curl 验证（对齐 FallbackModelRouterTest）
4. ChatClient 挂 `AuditLoggingAdvisor`
5. **禁止**重实现熔断状态机
6. README（无中间件；双 Key；说明 forceFail / status）+ api.http（chat、status、forceFail）
  </action>
  <verify>
    <automated>mvn -f examples/48-fallback-demo/pom.xml -q compile</automated>
  </verify>
  <acceptance_criteria>
    - server.port=18048
    - pom 含 saa-learning-starter 与 deepseek starter
    - 源码含 reportFailure( 与 isFallbackActive 或 /fallback/status
    - 源码不含自写 FallbackModelRouter 类定义
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>48 compile 绿，降级 status 可演示</done>
</task>

</tasks>

<verification>
mvn -pl common,starter -am -q -DskipTests install && mvn -f examples/47-routing-demo/pom.xml -q compile && mvn -f examples/48-fallback-demo/pom.xml -q compile
</verification>

<success_criteria>
- 47/48 均可独立 compile
- ModelRouter 来自 starter；双模型 Bean 名满足装配条件
- 48 暴露 /fallback/status 与 reportFailure 路径
- 47 冒烟 IT 就位
</success_criteria>

<output>
Create `.planning/phases/03-48-demo/03-13-SUMMARY.md` when done
</output>
