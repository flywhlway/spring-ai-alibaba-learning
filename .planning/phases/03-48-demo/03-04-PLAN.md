---
phase: 03-48-demo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/20-structured-output-demo/
  - examples/21-json-schema-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 真机 curl 需 AI_DASHSCOPE_API_KEY；编译不依赖 Key
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 20~34"
    - "D-03: 20~34 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用 PromptChatMemoryAdvisor/CallAroundAdvisor/FunctionCallback/可变 Options setter"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-09: common + Result + GlobalExceptionHandler"
    - "D-12: 规格以 docs/tutorial/16-StructuredOutput.md 可运行 Demo 为准"
    - "D-13: 20/21 → 教程第 16 章"
    - "D-16: 20/21 README 写无中间件"
    - "D-18: 20 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - 20 演示 .entity(Record) + validateSchema，端口 18020
    - 21 演示 ParameterizedTypeReference 嵌套泛型与校验容错，端口 18021
    - 两 Demo 均 compile，无硬编码密钥、无废弃 API、无 TODO
  artifacts:
    - examples/20-structured-output-demo/pom.xml
    - examples/20-structured-output-demo/README.md
    - examples/20-structured-output-demo/api.http
    - examples/21-json-schema-demo/pom.xml
    - examples/21-json-schema-demo/README.md
    - examples/21-json-schema-demo/api.http
  key_links:
    - 20/21 → docs/tutorial/16-StructuredOutput.md
    - D-01 本批仅 20~34
    - D-09 common + Result
---

<objective>
新建 20-structured-output-demo 与 21-json-schema-demo，演示 ChatClient 结构化输出全谱系。

Purpose: 交付次批 Structured Output 能力域。
Output: 两个可编译、可 curl 的独立 Demo。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@.claude/skills/saa-conventions/SKILL.md
@docs/tutorial/16-StructuredOutput.md
@examples/04-chat-demo/
</context>

<threat_model>
## Trust Boundaries
- 用户查询参数 → ChatClient → 模型 → BeanOutputConverter → REST JSON

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-20-01 | MED | 模型输出恶意/畸形 JSON | `validateSchema()` + Record 强类型；响应经 `Result<T>`，不执行原始字符串 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 20-structured-output-demo</name>
  <files>examples/20-structured-output-demo/</files>
  <read_first>
    - docs/tutorial/16-StructuredOutput.md（可运行 Demo 小节）
    - examples/04-chat-demo/pom.xml
    - examples/04-chat-demo/src/main/java/com/flywhl/saa/chat/ChatDemoApplication.java
    - .claude/skills/saa-conventions/SKILL.md
  </read_first>
  <action>
新建完整工程 `examples/20-structured-output-demo/`：
1. pom：parent 仓库根、`spring-boot-starter-web` + `validation` + `spring-ai-alibaba-starter-dashscope` + `saa-learning-common`，零版本号
2. `server.port=18020`；`spring.ai.dashscope.api-key: ${AI_DASHSCOPE_API_KEY}`
3. 包 `com.flywhl.saa.structured`；`@author flywhl`
4. 按教程实现 `DiagnosisResult` record + Controller：`GET /diagnose/structured?dtcCode=`，使用 `.entity(DiagnosisResult.class, spec -> spec.validateSchema())`，返回 `Result<DiagnosisResult>`
5. `@Import(GlobalExceptionHandler.class)`
6. README（前置条件：无中间件）+ api.http + curl 预期输出
7. 冒烟 IT：`@EnabledIfEnvironmentVariable(named="AI_DASHSCOPE_API_KEY", matches=".+")`，断言 entity 非空字段
禁止：硬编码密钥、TODO、`CallAroundAdvisor`、可变 Options setter
  </action>
  <verify>mvn -f examples/20-structured-output-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - examples/20-structured-output-demo/pom.xml 存在且 parent relativePath 为 ../../pom.xml
    - application.yml 含 server.port: 18020 与 ${AI_DASHSCOPE_API_KEY}
    - 源码含 .entity( 与 validateSchema
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>20 compile 绿，规格对齐教程 §可运行 Demo</done>
</task>

<task type="auto">
  <name>Task 2: 新建 21-json-schema-demo</name>
  <files>examples/21-json-schema-demo/</files>
  <read_first>
    - docs/tutorial/16-StructuredOutput.md（泛型/嵌套/校验段落）
    - examples/20-structured-output-demo/（Task 1 产物）
    - examples/README.md（21 演示要点）
  </read_first>
  <action>
新建 `examples/21-json-schema-demo/`（端口 18021，包 `com.flywhl.saa.jsonschema`）：
1. 依赖同 20
2. 演示嵌套 Record + `ParameterizedTypeReference<List<...>>` 的 `.entity(...)`（禁止 `.entity(List.class)`）
3. 至少一个接口展示 schema 校验失败时的容错/重试路径（教程 validateSchema 语义）
4. `Result<T>` + GlobalExceptionHandler
5. README + api.http
禁止废弃 API / 硬编码密钥 / TODO
  </action>
  <verify>mvn -f examples/21-json-schema-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - server.port=18021
    - 源码含 ParameterizedTypeReference
    - README.md 与 api.http 存在
    - compile 退出码 0
  </acceptance_criteria>
  <done>21 compile 绿</done>
</task>

</tasks>

<verification>
mvn -f examples/20-structured-output-demo/pom.xml -q compile && mvn -f examples/21-json-schema-demo/pom.xml -q compile
</verification>

<success_criteria>
- 20/21 均可独立 compile
- 端口 18020/18021，common+Result，密钥仅环境变量
</success_criteria>
