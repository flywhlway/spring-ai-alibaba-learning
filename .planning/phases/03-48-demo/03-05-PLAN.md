---
phase: 03-48-demo
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/22-embedding-demo/
  - examples/23-pgvector-demo/
  - examples/24-milvus-demo/
  - examples/25-redis-vector-demo/
  - examples/26-es-hybrid-demo/
autonomous: true
requirements:
  - REQ-phase-3-demos
user_setup:
  - 23 真机需 bash scripts/infra.sh up core
  - 24 真机需 bash scripts/infra.sh up vector（Milvus 冷启动 30~60s）
  - 25 真机需 redis/redis-stack-server（非 core 普通 redis）
  - 26 真机需 bash scripts/infra.sh up search
must_haves:
  truths:
    - "D-01: 本周期只交付/验收 Demo 20~34"
    - "D-03: 20~34 全部从教程规格新建"
    - "D-04: 包根 com.flywhl.saa.<模块>，@author flywhl"
    - "D-06: parent 指向仓库父 POM，零版本号"
    - "D-07: 禁用废弃 API"
    - "D-08: 零 TODO；README + api.http + REST + curl"
    - "D-09: common + Result"
    - "D-11: Embedding 一律 DashScope text-embedding-v4"
    - "D-12: 规格以 docs/tutorial/10-Embedding.md 与 11-VectorStore.md 为准"
    - "D-13: 22→10章；23/24/25/26→11章"
    - "D-14: 教程未给全码的 Demo 按同章 API 补齐"
    - "D-15: 23=core；24=vector；25=Redis Stack；26=search"
    - "D-16: 22 README 写无中间件"
    - "D-18: 22 冒烟 IT 使用 @EnabledIfEnvironmentVariable(AI_DASHSCOPE_API_KEY)"
    - "D-19: examples 独立应用，不挂父 POM modules"
    - 22 演示 EmbeddingModel 批量/维度/成本，端口 18022
    - 23~26 各自独立工程，统一 VectorStore.add + similaritySearch + Metadata Filter
    - 25 README 明确 Redis Stack，并提供 override 或启动命令
    - embedding dimensions 一律 1024，与 text-embedding-v4 对齐
    - 五 Demo 均 compile
  artifacts:
    - examples/22-embedding-demo/pom.xml
    - examples/23-pgvector-demo/pom.xml
    - examples/24-milvus-demo/pom.xml
    - examples/25-redis-vector-demo/pom.xml
    - examples/26-es-hybrid-demo/pom.xml
  key_links:
    - 22 → docs/tutorial/10-Embedding.md
    - 23~26 → docs/tutorial/11-VectorStore.md
    - D-11 Embedding=DashScope
    - D-15 infra profiles
---

<objective>
新建 Embedding + 四种 VectorStore Demo（22~26），体现统一 VectorStore 抽象与后端可移植性。

Purpose: 交付次批向量能力域（教学覆盖 ADR-004 四库）。
Output: 五个可编译独立 Demo；中间件依赖文档化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-48-demo/03-CONTEXT.md
@.planning/phases/03-48-demo/03-RESEARCH.md
@docs/tutorial/10-Embedding.md
@docs/tutorial/11-VectorStore.md
@docker/docker-compose.yml
@examples/04-chat-demo/
</context>

<threat_model>
## Trust Boundaries
- 用户 query/metadata → VectorStore filter DSL → 后端查询

## STRIDE
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-23-01 | MED | Metadata filter 注入 | 仅用 FilterExpressionBuilder；禁止把用户输入拼进原生 filter 字符串 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 22-embedding-demo</name>
  <files>examples/22-embedding-demo/</files>
  <read_first>
    - docs/tutorial/10-Embedding.md（可运行 Demo）
    - examples/04-chat-demo/pom.xml
  </read_first>
  <action>
新建 `examples/22-embedding-demo/`（端口 18022，包 `com.flywhl.saa.embedding`）：
1. 依赖：web + validation + dashscope + common
2. 按教程实现 EmbeddingBenchmarkController：`GET /embedding/benchmark?text=`，展示维度、耗时、向量长度；补充简单成本字段（如 token 估算或 dimensions 存储字节）
3. application.yml：`spring.ai.dashscope.embedding.options.model: text-embedding-v4`，`dimensions: 1024`，api-key 环境变量
4. README（无中间件）+ api.http
5. 冒烟 IT：有 Key 时断言向量非空
  </action>
  <verify>mvn -f examples/22-embedding-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - server.port=18022
    - 源码使用 EmbeddingModel
    - application.yml 含 text-embedding-v4 与 dimensions
    - compile 退出码 0
  </acceptance_criteria>
  <done>22 compile 绿</done>
</task>

<task type="auto">
  <name>Task 2: 新建 23-pgvector-demo</name>
  <files>examples/23-pgvector-demo/</files>
  <read_first>
    - docs/tutorial/11-VectorStore.md（§11.4 PGVector + 可运行 Demo Controller 模式）
    - docker/docker-compose.yml（postgres 凭据）
  </read_first>
  <action>
新建 `examples/23-pgvector-demo/`（端口 18023，包 `com.flywhl.saa.pgvector`）：
1. 依赖：`spring-ai-starter-vector-store-pgvector` + dashscope + common + jdbc（若 starter 未传递）
2. yml：datasource `jdbc:postgresql://localhost:5432/saa_learning` user/password `saa`/`saa123456`；`spring.ai.vectorstore.pgvector` dimensions=1024、initialize-schema=true、index-type=HNSW
3. REST：`POST /documents`（add）、`GET /search?q=&topK=`（similaritySearch），至少一个接口演示 FilterExpressionBuilder metadata filter
4. 返回 Result；README 声明 `bash scripts/infra.sh up core`
禁止拼接用户输入为原生 SQL/filter 字符串
  </action>
  <verify>mvn -f examples/23-pgvector-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - pom 含 spring-ai-starter-vector-store-pgvector
    - server.port=18023
    - 源码含 VectorStore 与 FilterExpressionBuilder 或 filterExpression
    - README 含 infra.sh up core
    - compile 退出码 0
  </acceptance_criteria>
  <done>23 compile 绿</done>
</task>

<task type="auto">
  <name>Task 3: 新建 24-milvus-demo</name>
  <files>examples/24-milvus-demo/</files>
  <read_first>
    - docs/tutorial/11-VectorStore.md（§11.3 Milvus）
  </read_first>
  <action>
新建 `examples/24-milvus-demo/`（端口 18024，包 `com.flywhl.saa.milvus`）：
1. 依赖：`spring-ai-starter-vector-store-milvus` + dashscope + common
2. yml：milvus host=localhost port=19530，collection-name、embedding-dimension=1024、index-type=IVF_FLAT、metric-type=COSINE、initialize-schema=true
3. REST：add + search，响应包含 score（若 API 可得）
4. README：`bash scripts/infra.sh up vector`，注明冷启动 30~60s
  </action>
  <verify>mvn -f examples/24-milvus-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - pom 含 spring-ai-starter-vector-store-milvus
    - server.port=18024
    - embedding-dimension 或 dimensions 为 1024
    - README 含 infra.sh up vector
    - compile 退出码 0
  </acceptance_criteria>
  <done>24 compile 绿</done>
</task>

<task type="auto">
  <name>Task 4: 新建 25-redis-vector-demo</name>
  <files>examples/25-redis-vector-demo/</files>
  <read_first>
    - docs/tutorial/11-VectorStore.md（Redis Stack 警告）
    - docs/tutorial/08-Memory.md（Redis Stack vs 普通 redis 说明）
    - HANDOFF-TO-CLAUDE-CODE.md（Redis Stack 注意点）
  </read_first>
  <action>
新建 `examples/25-redis-vector-demo/`（端口 18025，包 `com.flywhl.saa.redisvector`）：
1. 依赖：`spring-ai-starter-vector-store-redis` + dashscope + common
2. yml：连接 Redis Stack（默认 localhost:6379 或 override 端口，README 写清）
3. REST：add + search；可选语义缓存/TTL 演示端点
4. **必须**提供其一：`docker-compose.override.yml`（`redis/redis-stack-server`，避免与 core 普通 redis 冲突可用 6380）或 README 醒目手动启动命令
5. README 明确：**禁止**直接复用 core profile 的 `redis:7.4-alpine`
  </action>
  <verify>mvn -f examples/25-redis-vector-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - pom 含 spring-ai-starter-vector-store-redis
    - server.port=18025
    - README 含 redis-stack-server 字样
    - compile 退出码 0
  </acceptance_criteria>
  <done>25 compile 绿，Stack 依赖文档化</done>
</task>

<task type="auto">
  <name>Task 5: 新建 26-es-hybrid-demo</name>
  <files>examples/26-es-hybrid-demo/</files>
  <read_first>
    - docs/tutorial/11-VectorStore.md（Elasticsearch + Hybrid Search）
  </read_first>
  <action>
新建 `examples/26-es-hybrid-demo/`（端口 18026，包 `com.flywhl.saa.eshybrid`）：
1. 依赖：`spring-ai-starter-vector-store-elasticsearch` + dashscope + common
2. yml：ES `localhost:9200`，dimensions=1024
3. REST：add + 向量检索；至少一个端点演示全文/混合检索能力（按 Spring AI 1.1.2 ES VectorStore API 能力实现，README 说明）
4. README：`bash scripts/infra.sh up search`
  </action>
  <verify>mvn -f examples/26-es-hybrid-demo/pom.xml -q compile</verify>
  <acceptance_criteria>
    - pom 含 spring-ai-starter-vector-store-elasticsearch
    - server.port=18026
    - README 含 infra.sh up search
    - compile 退出码 0
  </acceptance_criteria>
  <done>26 compile 绿</done>
</task>

</tasks>

<verification>
for d in 22-embedding-demo 23-pgvector-demo 24-milvus-demo 25-redis-vector-demo 26-es-hybrid-demo; do mvn -f "examples/$d/pom.xml" -q compile || exit 1; done
</verification>

<success_criteria>
- 22~26 均可独立 compile
- 四库 artifact 正确、dimensions=1024、25 声明 Redis Stack
</success_criteria>
